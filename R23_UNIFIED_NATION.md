# R23 区域统合到统一国家的文字纵切片

## 目标

R23 让同一玩家政体从已完成内部统合的区域网络，进入可验证的统一国家阶段。它不是宣布胜利的剧情按钮，也不创建第二主权势力、固定地名或空间地图；每一项结果都继续写入国家内核的能力、行政区域、设施、网络、城市服务与因果账本。

## 阶段门

统一国家不是单一指数达标。完成条件必须同时具备：

1. 已完成 `capability.internal-integration`，且三个既有常驻城市都留在同一玩家政体；
2. 城市被登记为至少两个可追溯的内部行政区域，人口、服务与申诉仍由中央政体的统一登记处理；
3. 已运行一条连接跨区端点的可维护物流网络；
4. 已投用跨区行政设施，并具备可复制的国家登记与调度能力；
5. 已完成有期限的公共服务与统合程序，确认服务责任、申诉上报和共同保障优先级；
6. 统一章程完成后，政体才从 `regionalState` 转为 `nationState`，并取得 `capability.unified-nation`。

这六项分别对应行政、物流、设施、服务、社会认受性与单一主权；任何一项缺失时都不能跳关。

## 事实层扩展

国家内核将增加两种通用、可序列化的效果：

- `region`：创建由既有城市组成、隶属同一政体的行政区域；
- `polityProfile`：在阶段门满足时变更既有政体的模板、类型、模拟层级与战略意图。

城市、设施和网络仍使用已有的稳定 ID 与 `GeoReference`；R23 不生成坐标、不读取显示名、不把 UI 排版作为逻辑输入。行政区域不拥有独立外交、军队、货币、战略资源或退出权。

## 最小事项链

1. **跨区登记方法**（研究）：把多节点人口、服务责任与维护义务纳入统一登记，获得国家登记能力。
2. **国家调度所**（工程）：在既有核心城市投用可维护的跨区调度设施，形成行政承载实体。
3. **干线补给网**（工程）：连接既有内部节点，形成具有端点、容量与维护责任的跨区网络。
4. **公共服务保障令**（有限期政策）：在登记、调度与干线基础上执行，逐日提升各行政区域的服务与执行质量；它占用行政编制，结束后只留下制度化能力，不留下永久百分比加成。
5. **统一国家章程**（有限期政策）：确认统一登记、跨区调度、服务申诉和共同保障责任；完成后执行政体转型。

## 因果与日常生活

- 登记方法把临时名单变成可追溯的服务责任：家庭知道迁徙后由谁提供供水、诊疗与申诉入口。
- 调度所将维修、班次与物资协调从临时协调变为可维护机构，但也持续占用行政与维护人员。
- 干线补给网使节点不必只依赖临时队伍，却要求稳定检修和公共资源优先级。
- 服务保障令会改善跨区服务覆盖，同时挤压短期行政余量；它是有期限的行政行动而不是永恒加成。
- 统一国家章程把共同保障和申诉程序写入单一政体的执行结构；地方保留现场反馈与生活选择，但不成为并列国家。

## 验收

新增 `scripts/r23-unified-nation-sim.ts` 必须证明：

1. 未完成区域统合时，R23 事项全部不能启动；
2. 事项必须依照能力 → 设施 → 网络 → 有期限政策 → 政体转型的顺序完成；
3. 统一后城市总人口不变、城市归属和地理引用不被改写，且所有行政区域仍属于同一玩家政体；
4. 服务保障令带来可观察的服务/执行改善及行政占用，而非无条件永久加成；
5. `validateNationKernel` 通过，因果账本包含新设施、网络、政策与政体转型；
6. R15、R20、R21、R22 既有模拟仍通过，且不启动星球空间层。

## 未冻结项

- 行政区域的长期数量、人口规模、建设成本和服务效率不是本纵切片的平衡结论；
- 外部势力、外交、战争、正式道路选线与空间回接不属于 R23；
- 多世界/多战役模板将在 R23 的数据链稳定后，替换当前文字战役夹具进行验证。

## 2026-08-18 实现与模拟结果

- 已实现 `region` 与 `polityProfile` 两种国家内核效果；前者只在同一政体内重新登记既有城市，后者记录模板、政体类型、模拟层级与战略意图转型。
- 已接入五项 R23 事项和文字战役呈现。国家事项在区域统合完成前不显示为可推进内容。
- `scripts/r23-unified-nation-sim.ts` 已验证：区域统合前无法启动国家登记；第 319 日完成统一国家；政体成为 `nationState` 并具有两个内部行政区域；总人口和稳定地理引用不变；因果账本包含政体转型。
- 模拟还验证公共服务保障令在执行期间将节点供水覆盖维持在 45，结束后三日回落至 35；它留下的是服务责任能力，而不是永久覆盖数值加成。
- 类型检查、生产构建、R15、R20、R21、R22 与 R23 模拟均通过。文字 UI 尚未进行人工试玩验收；构建仅报告既有主包超过 500 kB 的提示。
