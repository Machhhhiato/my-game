# P1-S02 任务包：河谷计划期结算闭环

> **状态**：已冻结；可将“实现与验证”部分交给 DeepSeek。  
> **任务性质**：G1 的第一个可试玩规则切片。它把现有的战略命令 UI 变成一条可复盘的因果链；不新增视觉体系。

## 1. 玩家结果

玩家在河谷首局选择主/辅方向、旗舰工程、法令与安全姿态，点击两次确认后，中央政府立即以**两个自然月为一个计划期**完成一次批量统筹。玩家必须能在同一屏幕上读到：

`我的命令 → 中央调配了什么 → 哪些资源/债务改变 → 工程推进多少 → 风险是否兑现 → 下期要处理什么。`

这是“批量挂机结算”证明版：一次确认压缩执行一个计划期，便于验证规则。连续时间、离线追赶、多事件选择和六年完整内容不在本任务内，不能用伪造的实时日期替代结算。

## 2. 范围与禁止项

### 必须实现

- 战略命令的**预览、二次确认、立即结算、时间推进、保存与报告**；
- 当前 7 类资源、6 项能力、6 类债务、4 项旗舰工程、5 项法令、3 档安全姿态全部接入；
- 旗舰工程进度、确定性风险事件、最多 40 条事件日志；
- 同种子、同一旧状态、同一命令得到完全相同的结算结果；
- v2 存档安全升级为 v3（保留 v2 原件并可迁移）；
- 一个独立的无 UI 模拟脚本，验证关键路线与确定性。

### 明确不做

- 不改地图、Canvas、地形、配色、CSS、面板比例或任何视觉语言；
- 不添加科技树、战斗、外交、人口迁徙、地方扩张、个体微操、随机文本生成；
- 不做连续自动结算、离线追赶、危机选项或完整六年平衡；
- 不引入依赖、不提交、不推送、不删除 `cts-save-v2`。

## 3. 存档与纯结算边界

### 3.1 v3 字段

将 `CampaignSaveV2` 升级为 `CampaignSaveV3`，`version: 3`，并新增：

```ts
projectProgress: Record<ProjectId, number> // 0..100，初始四项均为 0
reports: PlanPeriodReport[]                 // 最多保存 12 份
settlementCount: number                     // 初始 0
eventFlags: Record<string, boolean>         // 初始为空
```

`PlanPeriodReport` 至少保存：发生前的 `year/period`、本期命令快照、资源/能力/债务各自的变化量、旗舰工程前后进度、一个或零个事件、3 条原因文本、确定性 `seedKey`。

### 3.2 迁移与键

- 新主键：`cts-save-v3`。
- 读取 `cts-save-v3` 优先；若不存在而 `cts-save-v2` 存在，则把 v2 原始文本备份为 `cts-save-v2-pre-settlement-<ISO 时间>`，再迁移成 v3 写入新键。
- 迁移不改变或删除 `cts-save-v2`；迁移失败时提示/新建 v3，不得覆盖旧键。
- v1 的退役识别与备份逻辑保持不变。

### 3.3 纯函数

新增 `src/v2/simulation.ts`，导出不依赖 React、Zustand、Canvas、DOM、时间或 `Math.random()` 的：

```ts
settlePlanPeriod(state: CampaignSaveV3, command: PlayerCommandState): SettlementResult
previewPlanPeriod(state: CampaignSaveV3, command: PlayerCommandState): SettlementPreview
```

这两个函数均不得修改输入对象。`preview` 与 `settle` 的数值结果必须一致；前者只不写入存档。随机阈值如需要，使用 `seed + settlementCount + eventId` 派生的本地哈希（0..99），并记录 `seedKey`。

## 4. 固定结算顺序

所有增减先在临时 delta 表累计，最后一次性应用并 clamp。资源库存不得小于 0；债务、能力、工程均 clamp 到 0..100。

1. 固定账户收支：`income - demand`；
2. 主方向完整修正，辅方向取其资源/能力修正的 50%（主辅相同则辅方向视为无）；
3. 旗舰工程修正及进度；
4. 法令与安全姿态修正；
5. 根据结算后库存与姿态产生基础债务；
6. 应用一个最高优先级的条件事件及其明确代价；
7. 按 delta 写趋势，更新能力/债务/工程/人口，生成报告、日志、`settlementCount + 1`；
8. `period + 1`；期数超过 6 时设为 1 且 `year + 1`。只有此步骤可以推进日历。

## 5. 精确规则表

未列出的资源/能力/债务变化为 0。资源表的数字是每计划期库存 delta，不是长期生产率；`有效劳力`库存表达本期可调配余量，不能低于 0。

### 5.1 固定账户收支

| 资源 | 安全水 | 热量 | 土地资本 | 回收材料 | 精密备件 | 有效劳力 | 公共信用 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `income - demand` | -2 | -2 | -1 | -1 | -2 | -5 | +1 |

### 5.2 方向（主方向 100%，辅方向 50%）

| 方向 | 资源 | 能力/债务 | 报告关键词 |
|---|---|---|---|
| 生存 | 水 +3，热量 +2 | 社会 +1 | 配给与维修优先 |
| 均衡 | 水 +1，热量 +1，材料 +1 | 维护债 -1 | 分散风险 |
| 科技 | 备件 -1，劳力 -1，信用 +1 | 知识 +3，维护债 +1 | 校验与学徒挤占 |
| 工业 | 材料 +3，土地 -1，劳力 -2 | 物质 +3，生态债 +1 | 回收与工务挤占 |
| 军事 | 劳力 -2，信用 -1 | 强制 +3，军事债 +1 | 护运与警戒挤占 |
| 航天前瞻 | 备件 -2，信用 -2 | 知识 +1，统合债 +1 | 年代能力不足的远景备案 |

### 5.3 旗舰工程（仅选中项完整生效）

| 工程 | 资源 | 能力/债务 | 基础进度 |
|---|---|---|---:|
| 河谷净水续命 | 水 +5，备件 -2，劳力 -1 | 后勤 +3，维护债 -5 | 25 |
| 种源圃与菌蛋白室 | 热量 +5，土地 +3，水 -2，劳力 -2 | 社会 +2，生态债 +1 | 25 |
| 河谷工务所校准 | 材料 +5，热量 -2，劳力 -2 | 物质 +3，维护债 -6，生态债 +2 | 25 |
| 档案校验与短波信标 | 备件 -2，劳力 -1，信用 +3 | 知识 +4，信任债 -2 | 25 |

工程实际进度：

```text
效率 = clamp(0.55, 1.10,
  0.85 + (后勤韧性 - 30) / 200 - 维护债 / 500 - max(0, 劳力需求 - 劳力库存) / 100
)
本期进度 = round(基础进度 × 效率)
```

其中的“后勤韧性、维护债、劳力需求、劳力库存”读取**结算前**状态。报告必须显示 `工程名：旧进度 → 新进度（本期 +N；效率 X%）`。

### 5.4 法令与安全姿态

| 项目 | 修正 |
|---|---|
| 混合配给 | 信任债 -1 |
| 谨慎登记 | 信用 -1，住房债 -2，统合债 +1 |
| 学徒优先 | 劳力 -1，知识 +2，维护债 -1 |
| 基本劳动保护 | 材料 -1，劳力 +1，维护债 +2，信任债 -1 |
| 社区安置 | 材料 -2，信用 -1，住房债 -4，社会 +2 |
| 低戒备 | 劳力 +1，军事债 +2 |
| 护运优先 | 无额外修正 |
| 强化警戒 | 劳力 -2，备件 -1，强制 +1，军事债 +3 |

### 5.5 基础债务

以下使用“工程/法令/安全”修正后的库存：

- 维护债：备件 `< 24` 时 +2；备件 `< 12` 时额外 +1；
- 生态债：土地资本 `< 40` 时 +1；
- 住房债：有效劳力 `< 20` 时 +1；
- 信任债：公共信用 `< 40` 时 +2；
- 军事债：低戒备 +1、护运 +0、强化警戒 +2；
- 统合债：公共信用 `< 40` 时 +1。

### 5.6 条件事件（每期最多一条）

按表中从上到下的优先级检查。事件的数值代价直接应用并在报告中标明；每条事件只触发一次。没有符合条件时报告写“本期无新增硬事件；风险继续积累”。

| ID / 条件 | 代价 | 日志地点与摘要 |
|---|---|---|
| `filter_strain`：净水续命已选、备件 `<= 12`、未触发 | 水 -8，信用 -3，维护债 +2 | 翡翠河谷水网：滤芯拆借，净水储备被迫下调 |
| `acid_rain`：土地资本 `<= 22`、未触发、派生 roll `< 40` | 土地 -4，生态债 +5 | 南部酸雨带：酸雨冲刷试验地，土壤基质报废 |
| `ferry_dispute`：谨慎登记、信用 `<= 36`、未触发 | 信用 -5，住房债 +3，信任债 +4，临时人口 +4 | 旧渡口行旅营：登记争议扩大，临时安置压力上升 |

## 6. UI 接线（不设计新视觉）

允许改动既有组件的文本和数据绑定，但不得改动 `src/v2/render/**`、`src/v2/v2.css` 或布局结构。

- 底部二次确认条显示预览中的：旗舰工程、预计最紧张资源、预计最大债务变化、可能事件（或“无硬事件预警”）。
- 第二次“确认”调用 `settlePlanPeriod`，关闭确认条，刷新顶栏数值，打开现有“报告”操作窗。
- “报告”窗删除静态段落，显示最新报告的命令、工程进度、资源变化、债务变化、事件/风险和 3 条原因。无报告时显示“尚未结算计划期”。
- 资源账与国家概览直接读新状态；趋势由本期 delta 得到。
- 事件日志追加一条“计划期结算”信息和（若有）一条事件；维持最多 40 条。
- 现有速度按钮不可再单独改变年/期：本任务中时间只在结算时前进。可以保留按钮的外观和存储值，但不得让它造成未结算的日历跳期。

## 7. 验收与交付

### 自动验证

新增 `scripts/p1-s02-sim.ts`，使用 `tsx` 运行，至少断言：

1. 对同一新局连续克隆并以相同命令结算，报告、状态与 `seedKey` 深度相等；
2. 净水续命相较档案信标，安全水 delta 更高、精密备件 delta 更低；
3. 强化警戒相较护运优先，有效劳力 delta 更低、军事债 delta 更高；
4. 连续采用净水续命可在备件阈值后只触发一次 `filter_strain`；
5. 输入对象在 `previewPlanPeriod` / `settlePlanPeriod` 后不被修改；
6. 日历只在结算后推进一期，最多保存 12 份报告与 40 条日志。

执行：

```bash
npx tsx scripts/p1-s02-sim.ts
npm run typecheck
npm run build
git diff --check
```

### 人工试玩

1. 新开战役，记录第 1 年第 1 期的水、备件、维护债；选择净水续命并确认；
2. 立即确认已变成第 1 年第 2 期，顶栏水/备件变化，报告能解释工程、债务与资源 delta；
3. 再次结算净水续命，备件阈值触发“滤芯拆借”；其事件与实际代价同时出现，第三次不重复触发；
4. 重载页面，报告、进度、日历和事件标记保持；旧 `cts-save-v2` 仍存在。

### DeepSeek 完成信号

最终报告必须先写入 `.codex/review-reports/P1-S02-settlement-loop.md`，然后仅在全部命令与人工路径完成后作为最后动作执行：

```bash
node scripts/review-handoff.mjs mark \
  --task P1-S02-settlement-loop \
  --summary "P1-S02 河谷计划期结算闭环已实现并通过模拟、构建与试玩" \
  --report .codex/review-reports/P1-S02-settlement-loop.md \
  --artifact P1-S02_SETTLEMENT_SPEC.md \
  --artifact scripts/p1-s02-sim.ts
```

报告不得声称视觉通过；它只报告功能、构建、模拟、截图路径（如有）、已知限制和唯一待裁决项。
