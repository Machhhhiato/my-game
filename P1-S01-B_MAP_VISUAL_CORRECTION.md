# P1-S01-B：战略地图视觉矫正施工图

> **接收人**：DeepSeek（仅实现）  
> **性质**：P1-S01-A 后的最终一次视觉返工；不是概念讨论。  
> **优先级**：本文件 > `P1-S01_VISUAL_BASELINE.md` > 旧 UI 规格/现有代码。

## 0. 本次只修什么

保留 v2 数据、存档、面板、指令和锚定交互；只修地图本体：

1. 默认星球不再是“深海圆球 + 三块小色斑 + 巨大治理多边形”。
2. 中近景不再是“居中方形补丁 + 两侧深空 + 斜向半透明色带”。
3. 近景要读出河谷基础设施和 8–24 个功能区群，而不是一小撮符号。

不做新系统、科技树、逐格建造；不改顶部资源、底部指令或浮窗语义。

## 1. 第一优先：完整矩形地图视口

目前 `PlanetCanvas.tsx` 以 `size = min(width, height)` 固定中间方形 Canvas，导致中近景两侧出现深空。这必须先重构：

- Canvas CSS 必须是 `width: 100%; height: 100%`。
- 逻辑渲染尺寸必须从容器宽/高传给绘制与投影；**不得**只将 600×600 Canvas 以 CSS 横向拉伸。
- 所有依赖 `MAP_W`/`MAP_H` 的投影、命中、图层、节点锚点、补丁采样改为使用 `MapViewport { width, height }`。
- 远景球心为 `(width/2,height/2)`，球半径按 `min(width,height)`；中近景补丁覆盖整个 `width × height`。

1440×900 截图硬指标：远景球直径占地图可用高度 86–92%；中景 `zoom=3.2`、近景 `zoom=7.0` 的地形像素覆盖地图可用矩形 **≥96%**。中近景不得保留两个宽的深空侧边。

## 2. 远景：构造真实大陆，而非节点岛

默认镜头仍聚焦 `(lon=41.5, lat=16.0)`。在此完整半球内，用确定性宏观地形场（数学距离场、样条、低频噪声、矢量多边形皆可；禁止图片）形成：

| 地貌 | 范围/关系 | 远景结果 |
|---|---|---|
| 主大陆“河谷大陆” | `lon=-15..78`、`lat=-38..54`，海岸至少 5 处明显凹凸 | 占可见球盘 26–34%；三节点位于同一大陆内部，不得各成孤岛 |
| 东部岛弧 | `lon=78..106`、`lat=-12..34`，与主大陆间有海峡 | 至少 2 座岛，合计占球盘 4–8% |
| 西北高地/半岛 | `lon=-30..8`、`lat=18..56` | 形成第二个地貌重心，不能只是边缘碎片 |
| 北部山系 | 主大陆北部西北→东北 | 连续暗灰山脊和少量雪顶 |
| 南部干地/酸雨带 | 主大陆南部/东南缘 | 干地和污染是陆地的一部分，不能成为独立椭圆贴纸 |

默认半球陆地投影总面积 **32–42%**。必须有深海/浅海/陆架，以及草地/森林/干地/高地/雪线五种陆地色带。

远景信息必须减法：只显示三枚节点、极细河流/道路和低亮灯火。节点群与工程线占球直径不超过 12%。治理区域默认只绘 1px 边界与≤6%的淡填充；不得出现覆盖球盘超过 8% 的金/青治理多边形。

## 3. 中景：必须是可读河谷

中景中心为 `(41.5,16.0)`。在全球高度场上增加**确定性河谷微地形**，且先于任何治理/人口/生态叠层生成：

| 要素 | 必须位置 | 视觉结果 |
|---|---|---|
| 主河 | `(36.5,22.0)` → `(47.6,5.5)`，经第 07 号附近、外拓营、旧渡口南侧 | 连续青蓝曲线；中景宽 2–4px、近景 3–6px；至少两条支流汇入 |
| 西侧山脊 | `lon=34..38`、`lat=23..8`，与主河平行 | 连续暗灰/灰绿山脊，至少三处明暗坡 |
| 东侧山麓 | `lon=43..48`、`lat=22..7` | 低于西侧但可读，围出河谷 |
| 河谷平地 | 主河两侧 1–2 经纬度 | 更亮草地、湿地、低对比耕地条纹；向外过渡到较干/较暗地貌 |
| 道路/水网 | 道路外拓营→旧渡口；水网第 07 号→外拓营 | 沿河谷/山口；不得穿越山脊、海面或任意直线硬连 |

三节点 `(38.2,18.6)`、`(40.6,16.4)`、`(45.2,13.8)` 都必须落在河谷和道路可解释的位置。地貌永远优先于治理区域边界。

三层同时开启只用于中景验收截图。此状态下：政治填充≤8%，直辖是金色细实线，协作是冷青 8px 间距斜纹，争议是砖红细虚线；人口层只是节点周围 30–70px 的柔和点光；生态层可见河网/耕地/酸雨斑点。任何层不得遮断主河、道路或山脊，也不得形成大片倾斜矩形色带。

## 4. 近景：18 个可见功能区群

在 `zoom=7.0`、中心 `(40.6,16.4)`，绘制如下 **18 个** 6–18px 低亮轮廓、短线或小灯火团；它们不是可点的独立建筑，且每个至少贴近道路、水线或主河：

| 组别 | 数量 | 位置/表现 |
|---|---:|---|
| 外拓营居住片区 | 4 | 外拓营西北/东北/西南/东南；暖灰小团 + 2–4 暖金点 |
| 温室/耕作带 | 4 | 沿主河两岸；浅绿细长条，方向顺河，禁止方格 |
| 水处理/抽水 | 3 | 沿河及第 07 号—外拓营水线；青灰短条/小环 |
| 维修/材料场 | 2 | 靠道路且离开河岸；冷灰和锈金短矩形群 |
| 仓储/调度 | 2 | 道路与水线交会处；深灰块 + 细调度线 |
| 公共服务/医疗 | 1 | 外拓营核心；冷白低亮环 |
| 外围警戒 | 1 | 东南边缘；极低亮折线/哨点轮廓 |
| 第 07 号井口群 | 1 | 西北上游；冷白环与短放射线 |

近景截图至少能清楚分辨 12 组；画面不得只剩一个中央符号团。

## 5. 图层、层级与禁止项

- 图层保留独立多选，默认只开启“政治执行”；中景验收图显式开启三层。
- 山脊与相邻低地明度差为 12–25%；道路中景 1–2px、近景 2–3px；水线中景 2–4px、近景 3–6px；政治边界 1px 且不得粗于道路。
- 禁止：新增常驻大面板、卡片、说明文字、图片纹理、UI 装饰、DOM/PNG 非空即宣称视觉通过。

## 6. 截图与正式交接

生成以下 1440×900 截图：

- `screenshot-visual-far.png`：默认远景，仅政治执行层，无浮窗。
- `screenshot-visual-medium.png`：翡翠河谷中景，三层同时开启。
- `screenshot-visual-close.png`：外拓营近景，18 个功能区群。
- `screenshot-object.png`：外拓营对象窗锚定。
- `screenshot-ledger.png`：精密备件资源账锚定。

最后先写 `.codex/review-reports/P1-S01-B-map-visual-correction.md`（修改文件、命令结果、截图路径、客观限制、待裁决项）；再通过 typecheck、build 和 `git diff --check`；最后一个动作运行：

`node scripts/review-handoff.mjs mark --task P1-S01-B-map-visual-correction --summary "战略地图已按 B 施工图完成视口、远中近景视觉矫正" --report .codex/review-reports/P1-S01-B-map-visual-correction.md --artifact screenshot-visual-far.png --artifact screenshot-visual-medium.png --artifact screenshot-visual-close.png --artifact screenshot-object.png --artifact screenshot-ledger.png`

`mark` 必须是最后一个动作；不得手写完成 JSON 或报告视觉通过。
