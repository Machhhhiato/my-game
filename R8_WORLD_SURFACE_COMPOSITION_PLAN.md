# R8-D 世界地表合成器：固定地理，连续缩放

## 纠正的问题

R8-C 的素材库质量可用，但“按镜头散点即时绘制”的接入原型是错误方向：它让镜头移动时要重复判断和绘制大量图片，造成卡顿；更严重的是，同一座山在远景不存在、近景才突然出现，破坏了星球作为一个连续地理实体的感觉。

本阶段冻结以下原则：**地形先于镜头存在，缩放不改变世界。**

## 唯一地表事实

世界生成时执行一次：

```text
世界种子 + 地理蓝图 + 地貌资产库
  → 地表占地与朝向清单（可存档）
  → 世界地表合成图 / 缓存图集
  → 全景球面、中景区域、近景局部都从同一份地表取样
```

存档保存蓝图、资产键、位置、旋转、真实占地范围与世界版本；运行时可重建缓存图，不需要把大位图塞进存档。工程和城市是这张固定地表之上的增量层。

## 占地等级：按真实地理尺度，而非按镜头大小

| 等级 | 代表 | 在世界中的作用 | 可见性 |
|---|---|---|---|
| 宏观地貌 | 山系、海岸线、大沙漠、冰盖 | 跨区域的连续形状；由多张同族贴图编排为一片地貌 | 全景即可辨认轮廓与色块 |
| 区域地貌 | 山脊、森林块、河谷耕作带、河口 | 附着于宏观地貌，有明确方向与边界 | 中景可读结构，近景可看贴图细节 |
| 局部地貌 | 孤峰、断崖、湿地、小沙丘 | 补充局部变化，不承担大地形轮廓 | 近景才看清像素，但远景位置仍属于同一地表 |
| 人工变化 | 城市、道路、水坝、港口、矿场 | 唯一随推演变化的表层 | 节点/工程状态改变时局部重绘 |

因此，“山脉”不再是若干独立小山图标，而是一条先由世界蓝图定义走向、宽度和峰线，再由山脊/山肩/孤峰/雪冠拼合出的连续山系；全景看山系体量，中景看脊线，近景看岩层贴图。

## 渲染与性能规则

- **生成或世界改造时合成，镜头移动时只取样。** 禁止在每次拖拽、缩放或动画帧遍历地貌贴图并重新决定落点。
- 基础地表以固定世界坐标编译到缓存图集；全景使用球面投影采样该图集。中近景以**同一份种子、模块、山系占地与工程差异**作细采样，并按同一份占地清单叠加地貌资产；这避免把低分辨率世界图集硬放大成大方块，也不会改变任何山、林、河谷的位置。
- 只有世界生成、地表工程达到里程碑、城市升级或地图种子改变时，才标记相应区域为脏并重新合成。
- 城市是可变对象：城市轮廓随发展阶段替换；工程在合法锚点叠加。它们不重新生成自然山脉、河流或森林。
- 远景可使用预合成缩略层，近景可使用同一图集的高分辨率块；两者必须来自相同占地清单。

## R8-D 实施顺序

1. 定义地表占地清单：每个地貌模块生成连续范围、方向、贴图族、密度和层级，不再生成屏幕坐标散点。
2. 用这份清单一次性编译世界地表缓存，并把缓存失效范围绑定到地图种子与工程地表变化。**已完成。**
3. 让全景球面和区域切平面统一取自这一份世界事实；验证缩放前后山系、河谷和海岸位置一致。**已完成。**
4. 在此基础上制作并接入 20 张通用城市轮廓；再进入工程地标波次。**下一步。**

## 当前状态

- 24 张地貌资产：**已归档、已接入固定地表合成器。**
- `surfaceFeatures`：**已随世界蓝图保存**；当前首局种子生成 134 处固定占地，其中 33 处为沿预设脊线拼接的宏观山系。
- 当前散点贴图原型：**已退役，不应作为可玩视觉交付。**
- 中近景：**已改为固定地理事实的细采样**，不再放大低清世界图，也不按镜头重掷或重排地貌。
- 城市与工程资产：未开始生产，避免在错误的地表架构上继续堆素材。
