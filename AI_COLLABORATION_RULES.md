# Always Game · 跨终端 AI 协作共识

> 状态：强制执行
> 生效日期：2026-08-19
> 目标：让不同电脑、不同 AI 工具和不同会话都能从 Git 恢复同一项目事实。

## 1. 唯一共识

**任何准备提交并推送到 GitHub 的项目变更，都必须先检查并更新相关 AI 上下文文件；代码、设定、状态、决策和上下文更新必须进入同一次 Git 提交并一起推送。**

禁止先推代码或设定、以后再补上下文。否则另一台电脑或新会话会读取到不完整事实。

## 2. 提交前必须检查的文件

每次提交前，AI 必须逐项判断是否需要更新：

1. `AI_CONTEXT.md`
   - 仅当项目定位、硬规则、权威顺序、主阶段或长期工作方法变化时更新。
2. `CURRENT_STATUS.md`
   - 功能、内容、验证结果、当前任务、优先级或风险变化时更新。
3. `DECISION_LOG.md`
   - 出现新的重大决定、替代旧方案或明确否决方案时更新。
4. `IDEAS_POOL.md`
   - 出现有长期价值但尚未冻结的想法，或想法状态改变时更新。
5. `AI_CONTEXT/` 目录
   - 更新与具体实施阶段直接相关的 `CURRENT_STATE.md`、`NEXT_TASK.md`、`DECISION_LOG.md` 或其他导航文件。
6. 对应规格、实施记录、验证报告和 `DOCS_INDEX.md`
   - 来源文件、阅读入口或文档关系发生变化时同步更新。

“不需要更新”必须是检查后的结论，不能因为赶时间而跳过检查。

## 3. 标准提交顺序

1. 检查工作区状态、分支、远端和未提交差异。
2. 完成代码、设定、数据或文档变更。
3. 运行与风险相称的验证；区分自动验证与人工试玩。
4. 更新对应来源文件和实施记录。
5. 更新相关 AI 上下文与决策/想法状态。
6. 检查 Git 差异，确认没有把计划写成已完成，也没有泄露秘密或混入无关文件。
7. 将功能与上下文放入同一次提交。
8. 推送到 GitHub。
9. 核对本地提交与远端分支一致，并在交付中报告提交号、验证结果和未完成事项。

## 4. 提交前检查清单

```text
[ ] 本次变更的事实来源已更新
[ ] CURRENT_STATUS.md 与当前实现一致
[ ] 新的重大决定已进入 DECISION_LOG.md
[ ] 未冻结想法只进入 IDEAS_POOL.md
[ ] 相关 AI_CONTEXT/ 导航已同步
[ ] 已明确区分：计划 / 已实现 / 自动验证 / 人工验收
[ ] git diff 已审阅，变更范围正确
[ ] 验证结果已记录
[ ] 功能与上下文将在同一次提交中推送
```

## 5. 跨终端启动规则

在任何新电脑、新终端或新 AI 会话中：

1. 先同步 GitHub 最新分支，且不得覆盖未提交本地修改。
2. 阅读 `AGENTS.md` 和本文件。
3. 按顺序阅读 `AI_CONTEXT.md`、`CURRENT_STATUS.md`、`DECISION_LOG.md`、`IDEAS_POOL.md`。
4. 再读取与当前任务相关的冻结规格、R 系列记录和代码。
5. 聊天记录用于补充推导过程；Git 中的文件才是可审计事实来源。

## 6. 冲突处理

- 如果聊天与 Git 文件冲突，以最新已提交的决策和冻结规格为准。
- 如果顶层状态与具体实现记录冲突，检查提交历史和代码验证证据，不得自行猜测。
- 如果发现上下文遗漏，先补齐上下文再继续新的重大实现。
- 如果旧决定被替代，必须在 `DECISION_LOG.md` 记录替代关系，不能静默改写历史。

## 7. 允许的例外

以下操作可以不更新全部上下文，但提交前仍必须完成一次检查：

- 纯拼写、排版或链接修复，且不改变含义。
- 可证明不改变行为的机械格式化。
- 临时本地诊断且不提交 Git。

只要变更改变了行为、数据合同、世界设定、玩家体验、当前进度、验证结论或下一步，就不属于例外。
