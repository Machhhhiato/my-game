# P1-S01 界面固定规格：河谷外拓战略界面

> **版本**：1.0  
> **状态**：锁定，作为 DeepSeek 的界面验收依据。  
> **设计原则**：这是统一指挥共同体的战略桌面，不是殖民者建造菜单、网页仪表盘或卡牌游戏。

## 1. 视觉目标

玩家打开主界面时，三秒内必须看懂四件事：我在何时何地、存续资源是否危险、当前计划在做什么、星球上哪里出了问题。

界面情绪是“昏黄废土上的理性公共工程”：深蓝黑背景、低饱和地形、暖金色公共工程/本方重点、青蓝色水与信息、克制的红色危险。不能使用霓虹赛博配色、纯黑大面积空白、圆角卡片瀑布、夸张渐变或图标堆砌。

## 2. 桌面布局（固定）

桌面基准为 1440×900；最小支持宽度为 1180。三栏顺序与信息层级固定。

```text
┌──────────────────────────────── 顶部国家栏 64 px ────────────────────────────────┐
│ 余烬统合档案 / 第07号 · 河谷外拓 | 资源账户 | 余烬历与计划期 | 速度/设置          │
├────────────── 左侧命令栏 264 px ───────┬───── 中央 Canvas 星球态势 ─────┬──── 右侧详情 300 px ───┤
│ 国家方向                               │ 标题 + 地图层切换                │ 所选地区/城市名称       │
│ 旗舰工程                               │                                  │ 直接管辖/契约/争议状态  │
│ 基础法令                               │ 可旋转、缩放、点击的程序化球面    │ 4 个关键指标            │
│ 安全底线                               │ 城市、道路、水系、势力与风险      │ 地区执行报告            │
│ [确认本计划期]                         │                                  │ 下一项风险              │
├────────────────────────────────────────┴──────────────────────────────────┴────────────────────────┤
│ 可折叠：河谷统合报告 / 事件选择 / 战区简报（P1 仅前两者）                              │
└─────────────────────────────────────────────────────────────────────────────────────────────────┘
```

- 顶部栏：64 px 高，背景 `#101a26`，下边框 `#263545`。
- 左栏：264 px，背景 `#0e1722`，只放本计划期可以改变的指令；不得放资源曲线或百科内容。
- 中央栏：占剩余宽度，Canvas 是视觉主角，最小可读面积 560×560。
- 右栏：300 px，背景与左栏一致，只显示当前选择对象；未选中时显示国家摘要，不允许空白。
- 报告区默认收起，发生事件时自动展开为底部叠层；不可在中央画布上弹出遮挡地图的大对话框。
- 宽度 1180–1279 时右栏折叠为可展开抽屉；低于 1180 不属于本任务验收范围。

## 3. 顶部国家栏

从左到右固定为：

1. **身份块**：`余烬统合档案`，副标题 `第 07 号 · 河谷外拓`；
2. **资源账户**：安全水、热量、精密备件、有效劳力、公共信用。每项只显示图标/名称、库存、上/下行趋势；悬停才显示收入、需求、储备目标与主要原因；
3. **时间块**：`余烬历 N 年 · 第 N 计划期`；
4. **时间控制**：暂停、1×、2×、4×；
5. **设置**：保存、导入导出、无障碍/图例说明。

资源危险颜色只用于趋势和数值，不给整块按钮染色：正常 `#d9e6ee`，注意 `#efba74`，危险 `#dc796f`，重点/可用 `#f1d98d`。

## 4. 左侧命令栏

每个命令区标题使用 11 px 大写感字距（中文正常显示），颜色 `#91aac1`；选项是 40–44 px 高的矩形行，边框 `#31465a`，背景 `#152230`。已选项使用暖金细边框 `#dbb85c` 和暗金背景 `#2a291e`，不得使用亮金色整块填充。

### 4.1 国家方向

- 同时显示主方向与辅方向；六方向都可点选。
- 已选方向右侧显示 `主` / `辅`，未选方向不显示无意义数值。
- 点击仅改变“待确认命令”，不立刻改变挂机状态；左栏底部 `确认本计划期` 后生效。

### 4.2 旗舰工程

固定显示本期一项建设中工程和其余可选工程。每行只能显示：名称、阶段（未选/勘测/建设中/停滞）、一个核心瓶颈。不得显示施工百分比以外的装饰性统计。

### 4.3 基础法令与安全底线

- 基础法令为配给、接纳、教育、劳动保护、居住五项中的一个优先标签；详情在右栏/悬停展示，不展开成五组滑条。
- 安全底线采用低戒备/护运优先/强化警戒三档，并永远显示“平民设施保护：标准”。P1 不开放总动员或战争目标。

### 4.4 确认按钮

左栏底部固定暖金按钮：`确认本计划期`。点击后显示一个轻量确认条：

`本期优先：河谷净水续命；挤占：档案校验 1 期；主要风险：滤芯耗竭。`

用户再次确认后开始结算。不得使用二次模态弹窗。

## 5. 中央 Canvas 星球态势

### 5.1 相机与交互

- 逻辑画布保持 600×600；设备像素比最多 2；显示尺寸随中央栏缩放。
- 拖拽旋转，滚轮/触控板缩放，单击选中，双击聚焦，再次单击取消选择。
- 缩放范围 1.0–10.0：远景读城市，区域态势读势力涂色，近景读城市功能区群与局部风险。
- 初始镜头对准翡翠河谷；首次打开不可自动持续旋转，用户 8 秒无操作后才以极慢速度自转。

### 5.2 三个固定地图层

右上角只放三个文本按钮，顺序固定：`政治执行`、`城市人口`、`资源生态`。一次只激活一个；按钮规格为 28 px 高、深蓝背景、激活时青蓝边框。

| 图层 | 远景 | 区域态势 | 近景 |
|---|---|---|---|
| 政治执行 | 城市与主要势力 | 直辖实色、契约斜纹、争议虚线、外部势力色 | 地区执行质量、道路/水网与边界 |
| 城市人口 | 城市灯火/轮廓和等级 | 城市类型、人口压力、服务缺口 | 8–24 个功能区群、住房/医疗/教育状态 |
| 资源生态 | 水系、主要耕地/污染带 | 水源、农地、遗址、酸雨、污染与网络 | 地区补丁中的河谷/试验地/风险边缘 |

图例固定在左下角，使用文字 + 纹理/图标，不可只依靠颜色。

### 5.3 城市表现

城市在远景中按真实坐标锚定，但最小显示为 8 px 光点/图标；这属于可读性放大，不代表占地。城市层的近景才依 `footprintKm2` 生成轮廓。

P1 只出现三类节点：

| 节点 | 视觉 | 可点信息 |
|---|---|---|
| 第 07 号深层存续设施 | 冷白井口光环 + 地下设施符号 | 遗产模块、维护债、后方安全 |
| 翡翠河谷外拓营 | 暖金居住灯火 + 水网线 | 人口、住房、净水/热量、旗舰工程 |
| 旧渡口行旅营 | 低亮青色临时灯火 + 断续道路 | 接纳/贸易状态、临时人口、风险与信用 |

### 5.4 地图禁止项

- 不显示可逐格建造的房屋、农田、炮塔或居民移动。
- 不显示巨大无说明的六边形/方格网。
- 不使用全地图饱和势力色；地形必须仍可阅读。
- 不绘制真实地球轮廓或复刻《环世界》素材。余烬是程序化星球，只借鉴“战略落点地图”的信息层级。

## 6. 右侧详情栏

右栏永远对应当前选择对象。标题下使用一句状态文字，例如 `直接管辖 · 河谷应急协调会` 或 `协作契约 · 旧渡口行旅营`。

固定顺序：

1. 地区/城市名称与政治状态；
2. 四项关键指标：执行质量、住房压力、核心服务状态、主要风险；
3. 80 字以内地区执行报告；
4. 下一风险或项目瓶颈；
5. `定位到地图`（仅在抽屉模式出现）。

指标必须是解释性词语 + 数值/趋势，例如 `住房压力：偏高`；禁止只有无意义的 0–100 条。

## 7. 事件与报告界面

### 7.1 条件事件

事件从底部报告区上推，宽度不超过中央栏 70%。结构固定：事件图标、标题、发生地点、2–3 句事实、两到三个清楚标明代价的按钮。90 秒倒计时仅在“系统按现有法令执行”时显示，不采用惩罚式红色闪烁。

### 7.2 井口听证硬节点

第 6 年显示为全宽底部统合报告的首屏，而不是普通随机弹窗：

- 左：本阶段命令、资源账、人民账和风险账；
- 中：井口听证的事实链和不同群体意见；
- 右：三项程序选择及其长期制度标签；
- 选择后生成“公开库存 / 申诉席位 / 仲裁庭”遗产条目，并回到星球画布。

## 8. 视觉 token（固定）

| 用途 | 色值 |
|---|---|
| 应用底色 | `#0a1019` |
| 栏位底色 | `#0e1722` |
| 控件底色 | `#152230` |
| 分隔线 | `#263545` |
| 正文 | `#e6edf3` |
| 次级文字 | `#9fb1c0` |
| 水/信息/已选地图层 | `#75c7e8` |
| 公共工程/本期重点 | `#d4a848` |
| 正常/可用 | `#d9ebd1` |
| 注意 | `#efba74` |
| 危险 | `#dc796f` |

字体使用系统无衬线；标题 14–16 px，正文 12–13 px，说明 11 px。圆角最大 2 px；阴影只用于星球和悬浮报告，不用于常规卡片。
