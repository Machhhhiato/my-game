# P1-S01 界面固定规格：河谷外拓战略地图

> **版本**：2.0
> **状态**：锁定，作为 DeepSeek 的界面验收依据。  
> **参考层级**：借鉴《环世界》的落点地图与《群星》的战略外框；不复制其素材、图标、名称或具体界面。
> **设计原则**：地图是主界面；常驻的是低干扰的国家工具，具体信息与命令以点击后的浮窗呈现。这不是殖民者建造菜单、网页仪表盘或卡牌游戏。

## 1. 视觉目标

玩家打开主界面时，三秒内必须看懂四件事：我在何时何地、存续资源是否危险、当前计划在做什么、星球上哪里出了问题。其余信息不抢占第一视线，而应在玩家点选城市、势力、工程、风险或快捷入口时出现。

界面情绪是“昏黄废土上的理性公共工程”：深蓝黑背景、低饱和地形、暖金色公共工程/本方重点、青蓝色水与信息、克制的红色危险。不能使用霓虹赛博配色、纯黑大面积空白、圆角卡片瀑布、夸张渐变或图标堆砌。

## 2. 桌面布局（固定）

桌面基准为 1440×900；最小支持宽度为 1180。地图可见面积必须始终不少于整个应用的 70%；不得以常驻左右信息栏切割中央画布。

```text
┌────────────────────────────── 顶部国家工具栏 48 px ──────────────────────────────┐
│ 徽记 | 水、热量、土地、材料、备件、劳力、信用（可展开） | 日期/计划期 | ⏸ 1× 2× 4× │
├─ 左侧快捷入口 44 px ──────────────────────── 全屏程序化星球/区域地图 ───── 右侧观察器 ─┤
│ 概览 / 计划 / 工程 / 法令 / 报告             地形、河流、道路、城市、势力、风险     228 px │
│                                                 ↑ 图层与图例（小型叠层）       默认收起  │
│                                                                                             │
│ [可固定的事件日志 300×176]                  [点击对象时出现的锚定信息浮窗]              │
│                                                                                             │
│                             底部情境命令条：计划 | 方向 | 工程 | 法令 | 报告              │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
```

- 顶部栏为 48 px 高的**常驻国家工具栏**；只容纳资源、日期/计划期、暂停/速度、设置。背景 `#101a26`，下边框 `#263545`。
- 左侧是 44 px 宽的竖向**快捷入口轨**，只显示图标和悬停名称；点击后在其右侧展开临时窗口，不形成永久命令栏。
- 中央 Canvas 覆盖顶栏以下的全部剩余空间，是视觉主角；画布上显示丰富地形、河流、道路、城市标记、可读的势力与风险叠加。
- 右侧是 228 px 宽、默认收起的**观察器抽屉**，不是详情栏。它只列出已知节点、工程队、待处理事件或后期军队/舰队；单击条目使地图定位并弹出对象浮窗。
- 底部情境命令条固定居中；点击命令后从底部上推一块临时操作板，关闭或确认后回到完整地图。
- 事件日志固定在左下角，可收起为一行；它记录已发生事实和待办，不等同于事件选择窗口。
- 任何对象详情都以紧贴对象或左下角的**浮窗**出现；浮窗关闭后不占地图面积。只有井口听证、年度结算等硬节点可使用宽幅叠层。

## 3. 常驻国家工具栏

从左到右固定为：

1. **共同体徽记与态势**：`第 07 号 / 河谷外拓`。点击打开国家概览浮窗，不显示大段摘要。
2. **资源条**：安全水、热量、生物土地资本、回收材料、精密备件、有效劳力、公共信用。默认每项显示 16 px 符号、库存和细小趋势；点击任一资源在其正下方打开资源账浮窗，展示收入、需求、储备目标、主要来源与主要缺口。
3. **时间条**：`余烬历 1 年 · 第 1 计划期`；点击打开计划期进度和下一次自动结算说明。
4. **时间控制**：暂停、1×、2×、4×。当前速度始终有边框和文字双重标识；离线追赶或待处理事件时必须自动暂停。
5. **设置**：保存、导入导出、无障碍、图例说明。

资源危险颜色只用于趋势和数值，不给整块按钮染色：正常 `#d9e6ee`，注意 `#efba74`，危险 `#dc796f`，重点/可用 `#f1d98d`。资源浮窗一次只开一个，宽 300–340 px；点击空白处或 Esc 关闭。

## 4. 左轨、观察器与底部命令

### 4.1 左侧快捷入口轨

入口按固定顺序为 `国家`、`计划`、`工程`、`法令`、`科研`、`报告`；P1 未解锁的项目显示低亮轮廓并有原因提示，不能隐藏以营造“空功能”。

- 点击入口时，在左轨右侧打开宽 340–380 px 的临时窗口；同一时间只存在一个此类窗口。
- `计划`是本期主/辅方向、旗舰工程、法令、安全姿态的总览及待确认变更；它不是一列常驻按钮。
- 所有选项先写入“待确认命令”，由底部 `确认本计划期` 提交；关闭窗口不丢弃待确认状态。
- 窗口须有关闭按钮和 Esc 关闭；关闭后仍保留地图选择对象。

### 4.2 右侧观察器

- 默认隐藏，仅露出 28 px 把手；点击/快捷键可拉出 228 px 宽抽屉。
- P1 分组固定为：`地点 3`、`工程 1`、`待办`。不显示空洞的国家指标、人物头像或数值瀑布。
- 条目最多两行：名称 + 一句状态/风险。单击：地图聚焦并打开对应对象浮窗；双击不触发额外操作。
- 观察器是导航和提醒，不承担对象详情；用户关闭它后所有核心功能仍可使用。

### 4.3 底部情境命令条

底部中央固定为五个矩形文本按钮：`计划`、`方向`、`工程`、`法令`、`报告`；左端显示当前待确认项数量，右端显示 `确认本计划期`。

- `方向/工程/法令`打开与左轨对应的轻量操作板；两种入口必须进入同一状态，不能有两套数据。
- `报告`打开最近的地区执行报告；`计划`打开本期挤占、风险与确认摘要。
- `确认本计划期`先显示 1 行确认条：`本期优先：河谷净水续命；挤占：档案校验 1 期；主要风险：滤芯耗竭。` 用户再次确认才开始结算。不得使用二次模态弹窗。

## 5. 事件日志与对象浮窗

### 5.1 固定事件日志

左下角固定一个 300×176 px 的半透明深色日志窗，标题 `河谷调度记录`。默认显示最近 4 条记录和未读计数；每项使用时间、地点/机构、事实摘要、严重性图标。示例：`第 1 期 · 河谷水网：滤芯储备低于目标，工务所已请求 2 单位备件。`

- 点击日志条目：定位关联地点，并打开相应对象或事件浮窗。
- 日志有 `展开历史`（临时 360 px 宽抽屉）和 `收起`（仅标题/未读数）两种状态；不能自动遮挡地图中央。
- 事件需要玩家决定时，日志条目带暖金边而非闪烁；点击打开选择浮窗。挂机在未选择时暂停，或在该事件规格允许时按现行法令处理。

### 5.2 对象浮窗

单击城市、设施、地区、势力范围、资源点、工程线或风险符号时，出现 280–340 px 宽的对象浮窗。优先锚定对象附近；若会越界或遮挡对象，改置于左下日志上方。

固定内容顺序：名称与类型 → 一句政治/执行状态 → 3–4 条解释性关键事实 → 当前瓶颈/下一风险 → 1–3 个合规行动。行动只能是战略级的“查看项目、优先勘测、发起接触、纳入下期计划”等，不能出现逐格建造或逐人指派。

### 5.3 重大事件与报告

- 条件事件为地图上的 360–440 px 浮窗：事件图标、标题、地点、2–3 句事实、2–3 个明确代价的选择；不以全屏红色警报替代解释。
- 第 6 年井口听证为宽幅底部叠层：左为命令/资源/人民/风险账，中为事实链和群体意见，右为三项程序选择；选择后写入制度遗产并回到地图。

## 6. 中央 Canvas 星球态势

### 6.1 相机与交互

- 逻辑画布保持 600×600；设备像素比最多 2；显示尺寸随全屏空间缩放。
- 拖拽旋转，滚轮/触控板缩放，单击选中，双击聚焦，再次单击空白处取消选择。
- 缩放范围 1.0–10.0：远景读城市与大陆格局，区域态势读势力、网络与生态，近景读城市功能区群与局部风险。
- 初始镜头对准翡翠河谷；首次打开不可自动持续旋转，用户 8 秒无操作后才以极慢速度自转。
- 远景到区域态势可保持球面投影；进入近景时允许平滑过渡到局部切平面，禁止只把低分辨率球面纹理放大。

### 6.2 地形优先的视觉构成

地形本身必须在无叠加层时完整可读：深浅海、水下大陆架、海岸线、河网、湿地、森林、草原、荒漠、耕地、山脊、雪线与污染痕迹。它们来自程序化高度、湿度、温度、河网与坡度，而非大块纯色格。

- 地形底图按种子生成并缓存；每帧只绘制相机变化和动态叠加，不能重新生成整星地形。
- 城市/设施在远景以最小可读标记显示；道路、水网、商路和警戒线遵循地形与节点，而不是直线随机涂抹。
- 政治色层只能以 20–35% 透明度、边界、斜纹或点纹出现，地形必须透出；不以全地图纯色涂块表现势力。
- P1 三个节点必须有不同可读符号：第 07 号为冷白井口环、外拓营为暖金聚居灯火、旧渡口为低亮青色临时灯火；真实坐标锚定，远景图标可为可读性放大。

### 6.3 固定地图层

地图右上角只放三个 28 px 高的文本层按钮：`政治执行`、`城市人口`、`资源生态`。一次只激活一个；基础地形、河网、城市和主干网络始终可见。图例为紧邻按钮的可收起小叠层，使用文字 + 纹理/图标，不可只依靠颜色。

| 图层 | 远景 | 区域态势 | 近景 |
|---|---|---|---|
| 政治执行 | 城市、主要势力和关系标记 | 直辖半透明色、契约斜纹、争议虚线、外部势力边界 | 地区执行质量、道路/水网与边界 |
| 城市人口 | 城市灯火/轮廓和等级 | 城市类型、人口压力、服务缺口 | 8–24 个功能区群、住房/医疗/教育状态 |
| 资源生态 | 水系、主要耕地/污染带 | 水源、农地、遗址、酸雨、污染与网络 | 河谷/试验地/风险边缘 |

### 6.4 地图禁止项

- 不显示可逐格建造的房屋、农田、炮塔或居民移动。
- 不显示巨大无说明的六边形/方格网。
- 不显示长期占位的左命令栏、右对象详情栏或底部数值仪表盘。
- 不绘制真实地球轮廓或复刻《环世界》《群星》素材；余烬是程序化星球，只借鉴其战略信息层级。

## 7. P1 对象与浮窗内容

| 节点 | 地图视觉 | 单击浮窗重点 |
|---|---|---|
| 第 07 号深层存续设施 | 冷白井口光环 + 地下设施符号 | 遗产模块、维护债、后方安全 |
| 翡翠河谷外拓营 | 暖金居住灯火 + 水网线 | 人口、住房、净水/热量、旗舰工程 |
| 旧渡口行旅营 | 低亮青色临时灯火 + 断续道路 | 接纳/贸易状态、临时人口、风险与信用 |

## 8. 视觉 token（固定）

| 用途 | 色值 |
|---|---|
| 应用底色 | `#0a1019` |
| 工具栏/浮窗底色 | `#0e1722` |
| 控件底色 | `#152230` |
| 分隔线 | `#263545` |
| 正文 | `#e6edf3` |
| 次级文字 | `#9fb1c0` |
| 水/信息/已选地图层 | `#75c7e8` |
| 公共工程/本期重点 | `#d4a848` |
| 正常/可用 | `#d9ebd1` |
| 注意 | `#efba74` |
| 危险 | `#dc796f` |

字体使用系统无衬线；标题 14–16 px，正文 12–13 px，说明 11 px。圆角最大 2 px；阴影只用于星球、浮窗和报告，不用于常规控件。
