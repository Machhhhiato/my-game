# P1-S01-A：河谷外拓战略地图视觉返工任务单

> **接收人**：DeepSeek Harness（实现工程师）  
> **任务性质**：在现有 v2 代码上做受控视觉返工；不是重新设计产品。  
> **视觉权威**：`P1-S01_VISUAL_BASELINE.md`。如本任务单与旧截图/旧实现有冲突，以视觉基准包为准。  
> **不得改动**：v2 战役数据含义、存档退役策略、顶部资源种类、左轨入口种类、底部指令语义、非本任务的业务结算。

## A. 完成目标

把当前“暗色圆球上有少量色块和叠层”的效果，改成玩家能直接读懂的、以连续地貌为主的战略行星地图。第一优先级是地图本身；UI 只做已经规定的锚定和克制处理。

最终视觉必须在不阅读文字的情况下给出三个答案：

1. **远景**：这是一颗有大陆、海洋、山系和气候带的完整行星。
2. **中景**：第 07 号、外拓营、旧渡口为何能通过河谷和道路互相联系。
3. **近景**：河谷的水、住区、生产、仓储与防卫以功能区群而非逐格微操的方式存在。

## B. 实施顺序与具体规则

### B1. 远景地形（先完成，不能跳过）

修改 `src/v2/render/terrain.ts`、`planetMap.ts` 及必要的样式，使默认 `zoom=1.5` 满足：

- 球体完整落在地图画布内，直径占可用地图高度 84–94%。球缘有细冷灰边与轻微背光/边缘暗影，但不可是硬白描边。
- 可见半球至少出现两块明显陆地或一主陆加一岛群；陆地投影约为球体面积的 25–55%。不要把节点所在地塑成唯一的几个圆形土块。
- 使用连续高度场塑造大陆尺度地貌：海沟/深海→陆架/浅海→海岸→低地→高地→山脊→雪线。大陆轮廓必须通过低频大陆尺度噪声、山系/裂谷带或同等连续场生成；局部节点保陆只能是微小修正。
- 大陆必须有至少两条连贯山系、一个雪区、森林/草地/干地三类气候区，以及至少一个有支流关系的河系。不要只增加随机小斑点。
- 海拔阴影基于真实采样高程的坡向，强度克制（明度变化建议 10–20%），不能把陆地压成黑色。
- 地图仍是程序化渲染：不可添加 PNG 地图、不可引入 Three.js/WebGL/网络依赖。

### B2. 中景河谷（远景通过后再做）

缩放到 `2.2–5.0` 时，使用原生/连续地形重新采样，而不是放大远景缓存。聚焦翡翠河谷时必须出现以下空间关系：

- 主河自北/西北高地向东南低地流动；至少两条支流接入。河两侧有较亮的湿润河谷带、耕地条纹或湿地，河外逐渐过渡到草地、林地或干地。
- 至少一侧为连续山麓/山口；道路跟随河谷和可穿越山口，不得横穿海、湖或高山脊直连节点。
- 第 07 号靠近可解释的井口/旧设施；外拓营在可耕、可取水的河谷位置；旧渡口在道路与水系交汇处。三者可用同一条道路/水网理解，但不必排成直线。
- 三图层同开时：治理层只用规定的低透明色、边界、斜纹/虚线；人口层是稀疏暖光密度；生态层强调河网、耕地与酸雨带。主河、道路、山脊仍必须优先可读。

### B3. 近景功能区（中景通过后再做）

在 `5.0–10.0`，根据节点和地区数据绘制 8–24 个**不可逐个经营**的功能区群。允许把生成逻辑写在 `planetMap.ts` 或独立模块；禁止新增方格操作、建筑队列或居民单位。

每个区群只是一组低对比度轮廓/灯火/设施线，且必须贴合道路或水网：

| 类型 | 视觉 token | 建议数量 |
|---|---|---|
| 避难所/公共服务 | 冷灰小型环状或矩形群，冷白低亮灯 | 1–2 |
| 河谷居住 | 暖灰、稀疏小团，暖金低亮灯 | 2–5 |
| 水处理/抽水 | 沿主河的青灰短条/小环 | 1–3 |
| 温室/耕作 | 河谷平地的半透明浅绿长条纹理 | 2–5 |
| 维修/材料 | 靠道路的锈金/冷灰长条 | 1–3 |
| 仓储/调度 | 道路交汇的深灰块与细线 | 1–2 |
| 外围防卫 | 地图边缘的一处低亮轮廓 | 0–1 |

节点之外不必显示文本。玩家点击的是地区/工程层级对象；单栋建筑、单户居民、炮塔、农田方格均不能成为对象。

### B4. UI 与锚定（只做基准包已定义内容）

- 保持：顶栏 48px、左轨 44px、左下 300px 日志、居中 40px 指令条、右侧 28px 观察器把手。
- 默认没有常驻右侧详情大栏。资源账在资源按钮下方 `4px` 锚定；对象窗在节点旁 `12–16px` 锚定，溢出才回退日志上方。
- 不添加新卡片、面板、动效、霓虹或装饰。不重新命名已有资源与动作。
- 保留并修正现有的三图层开关；每个开关切换后要有肉眼可见但克制的差异。

### B5. 性能与重绘

- 地形缓存只在 seed/图层/必要相机桶变化时重建；空闲画面不进行持续投影重绘。
- 拖拽中可用低分辨率预览，松手回全精度；不能在空闲时每帧重算地图。
- 不为了通过视觉任务引入重量级图形库。保持现有依赖清单不变。

## C. 禁止的“看似完成”方式

- 通过增加更多政治颜色、图标、文字来掩盖地形不可读。
- 用巨大右侧面板、全屏卡片或缩小地图来制造“信息丰富”。
- 只改 DOM/CSS，不改实际 Canvas 中远/中/近景的地貌可读性。
- 用 DOM 查询、截图存在、无控制台报错来宣布视觉通过。
- 在没有全部验收产物时运行完成信号脚本。

## D. 验收与正式交接

完成后，生成 **1440×900** 的如下截图（可扩展 `?shot=` 参数，但不要破坏正常入口）：

```text
screenshot-visual-far.png       默认远景、无弹窗
screenshot-visual-medium.png    翡翠河谷中景、三层均开
screenshot-visual-close.png     河谷近景、功能区群可读
screenshot-object.png           选中翡翠河谷外拓营的锚定对象窗
screenshot-ledger.png           精密备件资源账锚定资源项下方
```

然后执行：

```bash
npm run typecheck
npm run build
git diff --check
node scripts/review-handoff.mjs mark \
  --task P1-S01-A-visual-rework \
  --summary "河谷外拓战略地图已按视觉基准完成远中近景返工" \
  --artifact screenshot-visual-far.png \
  --artifact screenshot-visual-medium.png \
  --artifact screenshot-visual-close.png \
  --artifact screenshot-object.png \
  --artifact screenshot-ledger.png
```

前三条如有失败，修复后重跑；最后一条只能在前三条成功后作为**最后一个动作**运行。不要在开发中途运行它，也不要手动写 `.codex/review-ready.json`。

最终 Harness 回报只报告：修改文件、命令结果、截图路径、客观已知限制、需要裁决的问题。不要对视觉是否通过下结论，等待 Codex/创意负责人评审。
