# P1-S01 视觉基准包：河谷外拓战略地图

> **版本**：v1.0（已冻结）  
> **适用范围**：P1-S01 及后续所有星球战略地图切片。  
> **权威顺序**：本文件的可执行文字规格 > 本目录参考图的构图/气质 > 任何实现者的临场判断。  
> **实现边界**：这是一套 Canvas/CSS/程序化地形的视觉目标；参考图不是运行时贴图，禁止将其打包、切片或作为背景直接使用。

## 0. 参考资产

下列文件是创意负责人确认的风格锚点。它们用于人类评审；由于实现代理没有识图能力，本文件后续章节已经把必须复现的特征全部转写为文字、尺寸、颜色和验收条件。

| 视距 | 资产 | 视觉任务 |
|---|---|---|
| 远景 | `design/visual-baseline/p1-planet-far.png` | 一眼看出真实、有气候与地貌差异的行星，而不是色块球 |
| 中景 | `design/visual-baseline/p1-river-valley-medium.png` | 一眼读出河谷、山口、水网、道路和势力活动范围 |
| 近景 | `design/visual-baseline/p1-river-valley-close.png` | 一眼读出功能区群及其生产—居住—运输关系 |

近景图中的常驻大右栏、逐栋建筑和密集图标**不是**产品布局目标。本游戏保持“地图为主、信息按需弹出、指令只到战略层级”的原则。

## 1. 必须传达的视觉叙事

玩家扮演刚离开第 07 号避难所的统一政府，而非经营几名殖民者。画面必须同时传达以下事实：

1. 余烬是一颗完整、可居住但伤痕累累的行星；海洋、陆块、山系、气候带和河网有连续的地理逻辑。
2. 翡翠河谷只是巨大世界中的一个脆弱起点：水、道路、聚落、旧渡口和污染带之间存在可读的空间关系。
3. 国家控制并非把地图涂满高饱和颜色。直辖、协作、争议是**薄而克制的治理叠层**，底下的地貌永远优先可读。
4. 放大后信息密度提高的是“地区功能与基础设施”，而不是进入逐人、逐格或逐栋建造小游戏。

若任一视距只能读出 UI、色块、政治涂层或图标而读不出地理，则视为不合格。

## 2. 固定框架与信息层级（1440 × 900 基准）

### 2.1 永驻部分

| 部件 | 尺寸与位置 | 内容 | 禁止事项 |
|---|---|---|---|
| 顶部国家工具条 | 高 `48px`，全宽 | 徽记/战役名；7 种资源；日期与计划期；暂停、1×、2×、4×；设置 | 不得扩成第二行、不得使用大卡片 |
| 左侧快捷轨 | 宽 `44px`，顶栏以下到底栏以上 | 国家、计划、工程、法令、科研、报告六枚单色线性图标 | 不得放文字菜单或常驻面板 |
| 地图 | 其余全部可用面积 | 唯一主画面；不得被固定左右仪表盘挤压 | 不得使用规则网格、六边格或地块格线 |
| 事件日志 | 左下，宽 `300px`，展开最大高 `176px` | 最近 3–5 条调度记录、待办数、收起控制 | 不得做成聊天窗口或遮住地理核心 |
| 指令条 | 底部水平居中，高 `40px` | 计划、方向、工程、法令、报告、确认本计划期 | 不得成为大型底部控制台 |
| 观察器 | 右边缘默认仅 `28px` 把手；展开宽 `228px` | 地点/工程/待办的导航索引 | 默认不得常驻展开，不得承担详情阅读 |

弹窗是唯一详情载体：资源账从被点资源的下沿向下展开；地点/工程详情贴在对象旁 `12–16px`；若越出画布或对象不可见，才回退到事件日志上方。单个详情窗建议宽 `312–336px`，最大高 `420px`，同一时刻只允许一个主详情窗。

### 2.2 界面材质与文字

- 深空背景：`#070D15`；只在球外可见。可用稀疏星点、极低对比度银河雾带，绝不使用纯黑大空洞。
- 面板底：`#0E1722`，悬浮面板 `#142130`，边框 `#2B4153`；不透明度为 92–96%，可略透地图但不影响文字。
- 主文字：`#E6EDF3`；次文字：`#A7B8C7`；禁用：`#607181`。不用纯白大标题。
- 强调色仅两种：政府/确认金 `#D4A848`，已选对象冷青 `#73CBE7`。危险色 `#B86158` 只用于风险和争议。
- 字号：工具条 `12–13px`，数值 `16–20px`，弹窗标题 `16px`，正文 `12–13px`；所有中文采用正常字重，避免赛博霓虹、发光描边和超大字。
- 图标全部是单色线性或极简实体符号，`18–22px`；不可混用 emoji、写实图标或多彩徽章。

## 3. 地图渲染总规则

### 3.1 图层顺序（从下至上）

1. 深空与球体边缘暗影。
2. 海洋深度、陆架、连续海岸。
3. 海拔、坡向阴影、山脊、雪线。
4. 气候/生物群落与低频纹理。
5. 河流、湖泊、灌渠和湿地。
6. 道路、旧铁路/渡口、输水线等基础设施。
7. 地区功能区群与工程轮廓（仅中近景）。
8. 政治执行、城市人口、资源生态三类可开关叠层。
9. 节点、选中环、标签、风险符号。

每层必须保留底层可读性。政治色块或哈希不得盖过河流、道路、山地阴影；标签与节点在同一局部范围最多显示 5 个，更多对象按缩放级别聚合。

### 3.2 地貌调色板（允许 ±10% 明度扰动）

| 要素 | 主色 | 必须具备的变化 |
|---|---|---|
| 深海 / 浅海 / 陆架 | `#0C2E43` / `#195B73` / `#4A8EA0` | 海岸附近渐浅，不能只有单一蓝色 |
| 湿地与河流 | `#73C7E8` | 河流有上游—支流—汇流关系，末端可形成湿地或三角洲 |
| 河谷草地 | `#718855` | 沿河更亮、更湿，不是椭圆绿斑 |
| 森林 | `#294B38` | 随高度与湿度渐变，边缘不规则 |
| 干地/荒漠 | `#A18453` | 与雨影和山脉相连，不孤立贴纸状 |
| 高地/山脊 | `#686A63` | 有明暗坡与连续脊线，山体不可只画三角形 |
| 雪线 | `#DCE5E2` | 只在高海拔或高纬，边缘受地形约束 |
| 道路/旧轨 | `#C39A5A` | 细、低饱和、连接实际节点；断路本身可表现衰败 |
| 污染/酸雨 | `#9B6E48` 与 `#7C6050` | 斑驳、半透明，只附着在工业/下风/低洼区域 |

实现允许用噪声、分形、距离场与矢量线生成；不允许用少量大色块、均匀重复的格状纹理或静态大图替代。

### 3.3 治理叠层

| 状态 | 表现 | 最大覆盖不透明度 |
|---|---|---|
| 直辖执行 | `#D4A848` 极淡填充 + 1px 金色实边 | 18% |
| 协作执行 | `#4F9CB7` 极淡填充 + 间距 `8px` 的 45° 细斜线 | 14% |
| 争议/拒不执行 | `#B86158` 极淡填充 + 细虚线边 | 16% |

三类层均默认开启且可独立关闭。它们表示执行关系，不代表国境线；任何情况下不得超过地貌对比度的 1/3。

## 4. 三档视距的硬标准

### 4.1 远景：行星态势（缩放 1.0–2.2，默认 1.5）

- 默认画面中球体直径应占地图可用高度的 **84–94%**，完整球缘清晰可见；不要只出现一块放大的圆弧。
- 可见半球至少有两块主要陆地（或一块主陆 + 一个大岛群），陆地总投影面积占球面约 **25–55%**；海岸线必须曲折连续。
- 至少可读出：深浅海、海岸、两条山系、一个雪区、森林/草地/干地三类气候区、河口/河谷至少一处。
- 地图中心朝向翡翠河谷所在半球；第 07 号、外拓营、旧渡口三个节点在地理上接近但不能完全重叠，彼此可由河流或道路解释。
- 节点图形：第 07 号为冷白井口环；外拓营为暖金小型灯火群；旧渡口为青色临时灯火。远景标签仅在选中/悬停时完整显示。
- 此档不画地区房屋、农田格子或密集小图标。信息重点是“世界有多大、我在哪里”。

### 4.2 中景：河谷战略（缩放 2.2–5.0）

- 从球面过渡至局部切平面时，必须重新采样程序化地形；不得简单放大远景位图。
- 画面核心是一条可追踪的主河：有支流从山地汇入，河两侧出现河谷耕地条带/湿地，旧渡口位于可解释的河流交叉点。
- 至少一侧有连续山麓或山口；道路优先顺河谷和穿越山口，不能随机直线穿过山脉/湖泊。
- 直辖/协作/争议层的边界与道路、水网、聚落之间需有空间逻辑：例如外拓营周边直辖，旧渡口沿线协作，酸雨带附近出现争议或生态限制。
- 城市人口层以暖色稀疏灯火密度和柔和光晕表示，不能用成排房屋图标。
- 生态层显示河网、耕地纹理、酸雨带。所有叠层开启后，主河、道路和山脊仍要在一眼内看清。

### 4.3 近景：地区功能（缩放 5.0–10.0）

- 显示 **8–24 个功能区群**，而不是数百栋独立建筑或可点击方格。
- 功能区群以低对比度、成片轮廓表达：居住/公共服务为暖灰小团；维修与材料为冷灰/锈金长条；水处理沿河；耕作/温室在河谷平地；仓储/调度接近道路；防卫设施只是一处边缘轮廓。
- 每个地区最多显示 1 个名称与 1 个风险/工程标记；点击对象打开战略级详情，**没有**单户、单人、单炮塔、单田块操作。
- 保持道路—水网—功能区的连通性。近景增加的是因果可读性，而不是装饰噪声。

## 5. 交互视觉状态

- 悬停节点：仅出现 `1px` 冷青细环与短标签，不跳动、不放大、不播放粒子特效。
- 选中节点：双层细环（内 `#73CBE7`、外 `#D4A848` 低透明），对象窗从节点右侧/左侧就近弹出；地图不自动猛跳。
- 资源账：锚定在被点资源项底边下方 `4px`，与按钮左边对齐；显示库存、趋势、储备目标、来源、缺口，不使用柱状卡片墙。
- 计划确认：第一次点击只显示一行摘要，第二次确认才写入调度记录；强调金色只用于当前焦点或确认，不得让整个底栏发亮。
- 事件日志：风险事件使用小色线/图标区分，点击定位目标并打开对象窗；日志本身始终让位于地图。

## 6. 明确禁止项

1. 纯色陆块、低清噪声放大、没有海拔/气候逻辑的“涂色星球”。
2. 固定占据 25% 以上画幅的左右信息栏、卡片墙、经营面板或资源仪表盘。
3. 赛博霓虹、高饱和政治涂色、强烈外发光、密集装饰边框。
4. 六边格、方格、逐格建造、逐人动画、逐栋住宅/农田/炮塔微操。
5. 把参考图、网络图片、WebGL/Three.js 或静态大地图当作实现捷径。
6. 由实现代理擅自添加新的视觉语言、布局或未批准的常驻功能。

## 7. 截图验收

每次涉及地图视觉的工作，必须在 `1440×900` 生成以下截图：

| 文件名 | 场景 | 必须核验 |
|---|---|---|
| `screenshot-visual-far.png` | 默认远景 | 完整球体、连续地貌、无弹窗、地图占主导 |
| `screenshot-visual-medium.png` | 翡翠河谷中景 | 主河/支流、山地、道路、三个叠层同时可读 |
| `screenshot-visual-close.png` | 河谷近景 | 8–24 个功能区群、连通基础设施、无逐格微操 |
| `screenshot-object.png` | 选中外拓营 | 对象窗锚定节点且不遮住中心地理 |
| `screenshot-ledger.png` | 点击精密备件 | 资源账锚定资源项下方 |

自动化只能检查 DOM、截图尺寸、元素位置、渲染耗时和没有报错；**自动化不能宣称“视觉已通过”**。视觉是否通过只由创意负责人/Codex 根据本基准包判断。

## 8. 给实现代理的执行顺序

1. 先实现远景地貌可读性，再实现中景河谷逻辑，再实现近景功能区群；不得反过来堆 UI。
2. 每完成一档，保留上一档可用并生成对应截图。
3. 不确定任何视觉决定时，停止并列出具体选项；不能自行设计。
4. 只在所有命令、全部截图、变更检查完成，并将最终回报写入 `.codex/review-reports/<任务编号>.md` 后，运行 `node scripts/review-handoff.mjs mark ... --report <该文件>`，交给 Codex 评审。中间进度不需要、也不得触发评审。
