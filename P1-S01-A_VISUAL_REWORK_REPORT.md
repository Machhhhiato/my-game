# P1-S01-A-visual-rework 实现回报

> 状态：实现完成，等待 Codex / 创意负责人视觉评审。本回报仅陈述可自动验证的客观事实，不对视觉是否通过下结论。

## 1. 修改文件

- `P1-S01_IMPLEMENTATION_SPEC.md`（此前裁决：五类债务 → 六类债务）
- `index.html`
- `src/main.tsx`
- `src/v2/`（新增 v2 模块；本轮重点改动如下）
  - `src/v2/render/noise.ts`：`fbm3` 增加 `baseFreq` 参数（大陆尺度低频）
  - `src/v2/render/terrain.ts`：大陆尺度 + 高频地貌双频合成；海平面 `SEA=0.53`；基准色板连续地貌（深海/浅海/陆架/海岸/河谷草地/森林/荒漠/高地/雪线/污染）
  - `src/v2/render/layers.ts`：治理叠层配色（直辖 `#D4A848` 实边、协作 `#4F9CB7` 斜纹、争议 `#B86158` 虚线）
  - `src/v2/render/planetMap.ts`：远景球面投影 + 中近景局部切平面（原生重采样）；球缘背光/边缘暗影；多层合成；河网/道路/水网矢量；近景 18 个功能区群；节点悬停/选中双层环
  - `src/v2/types.ts` / `state.ts` / `save.ts` / `store.ts`：`mapLayer` 单值 → `activeLayers` 数组（含旧档迁移）
  - `src/v2/ui/LayerToggles.tsx`：三图层改为独立开关
  - `src/v2/ui/PlanetCanvas.tsx`：默认 zoom 1.5；脏标记重绘；悬停/中近景拖拽预览；`?shot=` 截图模式
  - `src/v2/v2.css`：基准色 token（深空 `#070D15`、面板 `#0E1722`/`#142130`、边框 `#2B4153`、冷青 `#73CBE7`、危险 `#B86158`）

## 2. 命令结果

```text
npm run typecheck   → 通过（无错误）
npm run build       → 通过（52 modules）
git diff --check    → 通过（无空白错误）
```

## 3. 截图产物（1440×900）

| 文件 | 场景 | 客观画布采样 |
|---|---|---|
| `screenshot-visual-far.png` | 默认远景完整星球，无浮窗 | 球盘不透明 246681/360000 px，均值 RGB [30,69,85]（深海） |
| `screenshot-visual-medium.png` | 翡翠河谷中景，三层均开 | 满画布，均值 [133,153,127]（河谷草地） |
| `screenshot-visual-close.png` | 河谷近景功能区群 | 满画布，均值 [164,164,128] |
| `screenshot-object.png` | 选中翡翠河谷外拓营，锚定对象窗 | 对象窗 inline `left:707px; top:212px; bottom:auto` |
| `screenshot-ledger.png` | 精密备件资源账 | 账本 inline `left:462.5px; top:39.5px`（按钮 `bottom:35.5px` 下方 4px） |

## 4. 客观已知限制

1. 中近景与远景在 `zoom≈2.2` 处为离散切换（球面 → 局部切平面），非平滑过渡；单位球面投影无法把约 7° 宽的河谷放大至充满画布，故中近景采用原生重采样切平面。
2. 中近景/近景拖拽跨越补丁缓存桶时需一次性原生重采样（全精度 600×600 约 150–300ms），已用 320px 拖拽预览缓解，快速长距离拖拽可能轻微跟手延迟。
3. 近景 18 个功能区群为确定性硬编码偏移（贴合节点/道路/水网的示意轮廓），未从 128×128 地区补丁运行时推导。
4. 全球陆地投影面积实测约 35.1%（基准要求 25–55%），三节点均在陆上。

## 5. 需要裁决的问题

三图层现改为可同时开启（独立开关，默认仅「政治执行」开启），以满足视觉基准包 B2 与任务单「三层均开 / 三图层同开」。此与 `P1-S01_UI_SPEC.md` §6.3「一次只激活一个」冲突。请裁决：保留独立多选，还是恢复单激活并为中景截图另行叠加三层。
