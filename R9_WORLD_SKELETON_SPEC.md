# R9 世界骨架与工程地理协议

## 本轮目的

先验证世界是否像一个可推演的地球，而不是验证美术。`?skeleton=1` 只显示黑白海深、高程、坡向、等高线、河流、道路、工程落点与地点锚点；不加载城市、山脉或工程贴图，不使用政治／人口／生态图层。

## 固定世界骨架

- 使用二十面体 L5 细分网格：20,480 个稳定球面单元，平均尺度约 160 km。
- 单元 ID 为 `g5-<index>`，可由网格版本重建；存档不写入两万个重复对象。
- 每个单元有球面中心、经纬度与三条邻接关系。城市、工程、道路不得再以散落的屏幕坐标作为唯一位置事实。
- 已知地点以 `GeoReference.point` 绑定到单元，并保留格内偏移。之后同一地点换投影、换贴图或换镜头时仍落在同一地理位置。

## R9.1 类地球自然生成基线

- 世界蓝图中的 `landmasses` 是自然高程的输入，不再只是贴图摆放参考。
- 每个大陆块由克拉通、半岛和海湾的连续距离场组成；种子只扰动海岸细节与地貌，不允许多边形硬裁边。
- 高程按“深洋盆地 → 大陆架 → 低地 → 高原／山系”连续计算；流域沿程必须从源头向出口下降。
- 验收陆地覆盖率为 18%–46%；当前首局种子为 23.2%，刻意保留比地球更大的海域，给后续远洋、殖民与大陆间物流留空间。
- 生成器版本升级时，保留现有工程地表差异，以同一 seed 重建自然骨架；旧世界原文先备份。

## 四层数据，不可混写

| 层 | 记录什么 | 不记录什么 |
|---|---|---|
| `WorldBlueprint` / `WorldSkeleton` | 自然地理、流域、地貌模块、稳定格与地点锚点 | 建设进度、城市图像、玩家面板状态 |
| `MapModuleDefinition` | 可复用的地理模块：道路、设施、港口、矿场等的兼容地形和几何类型 | 某一局的坐标、完成日期 |
| `WorldEffectInstance` | 某局工程实际产生的地理结果、位置、阶段、来源工程 ID | 自然地貌的定义、科技或工程的说明文字 |
| 视觉层 | 读取前三层，选择黑白线条或以后统一贴图 | 任何决定性的地理／工程事实 |

当前 `terrainChanges` 是早期地表改造兼容层。后续 R9-S3 会将它迁入 `WorldEffectInstance`，届时老存档通过适配器迁移；本轮不重置存档，也不改写既有工程结果。

## 验收口径

1. 同一 seed 生成相同的单元、相邻关系、地点绑定和高程底图。
2. 流域由高处流向低处；城市／渡口／矿点的地理条件可由骨架字段验证。
3. 道路、水网和未来工程均引用地理对象，而不是渲染器内硬编码的一组屏幕位置。
4. 缩放只改变展示精度，不能重新抽样生成自然山脉、海岸或河流。
5. 玩家先验收黑白图的大陆尺度、海岸、山系与河谷；通过后才把地貌贴图按同一份事实层重新接入。

## 本轮非目标

- 不调整现有 UI、资源栏、政策／工程面板或地标贴图。
- 不展示每个 L5 三角单元的边界；它们是战略数据骨架，不应在全景变成噪点。调试格线将作为独立开发开关提供。
- 不做美术合成、城市资产与道路贴图；这些必须等待骨架验收。
