# R25 文字战役模板分离

## 目标

把首阶段的共享能力目录，与只用于展示和验证的探索地图夹具分离。默认试玩仍保持原有内容；模板不改变科技、工程、政策、区域网络或国家内核的语义。

## 契约

- `TextCampaignTemplate` 只提供探索目标的呈现、稳定地理引用、坐标和发现的放置位置。
- 科技、工程、政策和发现 ID 是跨模板复用的能力契约，不以方向、地名或坐标作为逻辑键。
- 存档保存 `campaignTemplateId`；v6 默认试玩存档会迁移为 v7 的默认模板。
- 从稳定聚居进入区域网络时只读取人口、储备、已完成能力和工程实例；不会改写模板地理引用。

## 验证夹具

`campaign.ridge-v1` 使用不同的目标 ID、名称、方向、坐标、地理引用与探索顺序。`scripts/r25-template-separation-sim.ts` 以该模板跑通：探索 → 研究 → 工程 → 稳定聚居 → 区域网络 → 区域统合 → 统一国家，并校验国家内核。
