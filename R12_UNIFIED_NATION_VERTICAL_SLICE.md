# R12 统一国家纵切片：国家内容包与实体结果

> 状态：实施规格。用于验证 R11 在“统一国家”尺度能否承载真实内容；不是正式战役、数值平衡结论或地图选址实现。

## 1. 范围

本切片在 `save.fixture.unified-nation` 中安装 30 个可执行定义：10 项科技、10 项工程、10 项政策。它们服务于一个已统一、但设施水平不均、维护压力存在、区域服务仍需整合的国家。

- 科技：授予可验证的国家能力，并作为工程或政策的前置。
- 工程：消耗 NDU 与工务编制，在完成时创建 `FacilityState` 或 `NetworkState`，或写入城市服务覆盖。
- 政策：是有限期行政行动；占用行政/执行人员，通过每日效果改变维护、服务、统筹或风险，期满后停止。
- 夹具中已有的发电、检修、港口和公共卫生设施代表既存事实；新工程不会凭空覆盖它们。

本切片不实现：真实地理选址、路线寻径、战争结算、完整自动排程、R10 正式存档迁移或玩家最终界面。

## 2. 内容链

```text
电网调度 / 供水标准 / 互换件体系 / 系统测绘
      ↓                    ↓
跨区互联线 / 海岸净水厂 / 北部检修中心 / 技术学院
      ↓                    ↓
网络运行 / 公共服务数据 / 城市卫生网络 / 韧性规划
      ↓                    ↓
货运走廊 / 登记链路 / 都市诊疗网 / 海岸防护工程
      ↓
维护更新、学徒、供水优先、货运协同、服务协定等有限期政策
```

每条链至少跨越“能力 → 实体工程 → 服务/维护/组织变化”两次边界。没有能力证明的工程不能启动；依赖实体工程的下一项科技/政策也不能只凭研究点跳过。

## 3. 静态定义与动态状态

`UnifiedNationOperationDefinition` 是静态内容，含稳定 ID、类型、前置、工期、编制、投入、效果和玩家文案字段。`OperationState` 是存档中的动态执行副本，保存进度、开始/完成日期与当前状态。

工程完成使用两类标准效果：

- `facility`：写入带权责、生命周期、维护编制、宿主城市和每日服务效果的设施；同步把 ID 写入城市设施列表。
- `network`：写入带端点、容量、状态、冗余和生命周期的网络；都市圈或地区可随后引用该网络。

内容包通过 `UnifiedNationSliceBindings` 接收玩家势力、城市角色与既有设施 ID。它不读取城市显示名、屏幕位置、河谷专名或视觉资产。

## 4. 初始国家事实

国家夹具包含三个地区、五座城市和三个都市圈。北部承担工业与港口，中央承担行政与国家调度，海岸承担科研与物流；这只是“城市角色”测试数据，不是地图剧情。

初始设施与网络必须能说明初始指标的来源：

- 中央基础电站、北部维修工坊、北部港口装卸设施、海岸基层诊疗点；
- 北部—中央的既有干线与海岸港口支线；
- 城市级供水、医疗、教育覆盖率按各地起点分别存储，不使用国家平均值替代。

## 5. 验收

1. 30 项定义按科技、工程、政策分别存在；它们都有 ID、前置、投入、编制、持续时间与效果。
2. 启动不满足能力/实体前置的项目必须失败且不扣除投入；满足后能稳定启动。
3. 完成工程会创建可追溯的设施或网络，并改变城市服务或国家能力；不得只是修改一个项目状态。
4. 政策的每日效果在期限结束后停止；科技能力能进入势力能力表。
5. 同一初始夹具与同一启动顺序，推进相同天数得到完全相同的状态和账本。
6. R11 检查器能看见上述对象、进度、量纲变化和账本原因；R10 默认试玩不受影响。
