# R38 工程地理事实骨架

> 状态：已实现并验证。此文记录 R38-C 中先于大规模内容回补建立的位置合同。

## 结论

工程不是“完成项目 ID”，而是可追溯的地点事实：

`勘察记录 → 工程候选地 → 施工中设施 → 投用设施 → Nation Kernel 设施/网络/城市`

所有位置通过稳定 `coordinateRef` 解析为 `GeoReference`，再由 3D 星球投影。旧文字图的 `mapPosition` 永远只用于旧界面排版，不能参与工程选址、网络判定、区域划分或存档迁移。

## 当前数据合同

`TextFacilityFact` 保存：项目 ID、坐标引用、候选地发现 ID（如有）、地图类别、施工/投用状态、开工日和投用日。

- 有工程候选地前置的项目，必须继承该 `engineering-site` 发现的 `coordinateRef`；
- 没有候选地前置的基础驻地项目，只能落在已有的 `home:<template>:<seed>` 驻地引用；
- 任何尚无勘察依据、也不属于驻地的未来项目必须显示为“待选址”，不得生成伪造位置；
- 旧存档会在下一日推进时补出已完成设施的位置事实，不修改已有能力、人口或项目完成状态；
- 进入区域内核时，驻地城市和每一项已完成设施继承同一位置。后续设施、网络、城市、舰队母港、发射场和空间资产沿用 Nation Kernel 的可选 `geoRef` 字段。

## 3D 表现

3D 星球现在分别绘制工程候选地、施工中工程和已投用设施。施工到投用只改变同一事实的生命周期，不创建另一处地图对象；位置不随镜头、模板 UI 布局或显示名称改变。

## 验证

`scripts/r38-globe-map-simulation.ts` 验证候选地选址、施工→投用、地图派生和三模板引用隔离。

`scripts/r38-template-horizon-simulation.ts` 使用内陆、山地/高地、沿海/大水域三套模板，均从早期战役进入统一国家并再推进十年（第 3970 日）。每套存档均确认设施 `GeoReference` 未在区域桥接中丢失，并通过 Nation Kernel 校验。

## 后续回补顺序

1. 给新工程内容声明候选地类型或明确驻地/待选址策略；
2. 给区域设施、网络端点和新城市写入既有 `GeoReference`；
3. 将地表设施、网络、区域服务和边界回接 3D 地图；
4. 最后才为港口、机场、发射场、舰队母港和空间资产增加地面锚点；轨道与星际空间仍不在 R38 范围内。
