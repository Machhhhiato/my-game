# R4 地图美术与设施表现规范

## 目标

在大地图上让玩家读出地形、聚居地和关键工程的变化；不把城市做成逐格建造，也不把数百个建筑贴图常驻内存。

> **边界说明（D-019）**：当前“翡翠河谷”四张图仅用于验证独立透明贴图、阶段切换和缩放层级，不能扩展成正式城市资产。正式城市必须是可跨地图复用的地形/气候型资产；其完整清单在 R5 完成每阶段 1,000 科技与 1,000 工程的分类后统一制定、审核、生成和接入。

## 三级表现

| 层级 | 代表 | 表现方式 | 资源上限 |
|---|---|---|---:|
| 地貌与网络 | 山脉、河流、道路、管线、农田、水库 | Canvas 连续地形与矢量网络 + 中近景透明地貌贴图 | 单张 ≤20KB |
| 聚居地轮廓 | 聚居点、小镇、城市等 | 通用城市阶段贴图 + 地点旗帜 + 规模圆环 | 单张 ≤20KB |
| 独立地标 | 水坝、矿场、科研所、医院、大学、通信塔 | 中景地点旗帜+规模圆环；近景透明底像素贴图+网络痕迹 | 单张 ≤20KB |

## 正式贴图规则

- 每一张正式贴图使用真实透明背景；禁止黑底、色键抠图或 `screen` 混合伪透明。
- 地貌贴图：48×48 或 64×64，透明底；按山脊、林簇、河谷等模块覆盖，不按格铺满；目标 2–12KB。
- 常规地标：24×24 或 32×32，约 12 色；目标 2–8KB。
- 常见城市地标：32×32 或 48×48；目标 4–15KB。
- 战略巨构：最多 64×64；单张仍不可超过 20KB。
- 用 `shape-rendering="crispEdges"` 或等价像素化绘制；Canvas 绘制时 `imageSmoothingEnabled=false`。
- 中远景（区域态势）显示聚居地的阶段贴图、地点旗帜和规模圆环；单项工程不在这一层抢占标签。
- 中景的外部地点（矿场、水坝、渡口等）显示各自贴图或旗帜、名称和规模圆环；近景才加载其独立设施贴图。
- 近景贴图必须从已确定的地标概念图抽稀、裁切而来，不得重新设计成抽象图标。
- 贴图只有在该设施进入试运行或投用、镜头进入近景且地图需要显示时才请求加载。
- `scripts/check-map-assets.mjs` 是提交前体积门：`src/v2/assets/` 下任一地图贴图超过 20KB 即失败。

## 状态与地图的唯一事实源

`FacilityRuntime.stage` 决定地图，而不是项目 UI 自行判断：

```text
locked         不显示设施
planned        轻量虚线轮廓
construction   施工线、地基或脚手架
trial          小贴图 + 冷色测试环
operational    完整贴图 + 网络/灯火效果
damaged        受损轮廓与风险标记
```

## 正式城市资产矩阵（R5 内容门完成后执行）

城市不是剧情地点插画，也不把河谷、设施编号或单一条河写进贴图。正式资产由地图属性选择：

```text
城市贴图 = 气候/地貌型 × 城市发展阶段 × 季节 × 天气
```

- **气候/地貌型**：至少覆盖温带内陆、干旱内陆、热带/丛林、高地/山地、极地/寒带；沿河、湖畔、海岸等只是地图锚点与背景网络条件，不能将同一条河固化进城市主体。
- **发展阶段**：由完整编年史的聚居规模定义并另行冻结；R4 的“营地/定居点/工务镇/城市”只是技术验证，不是正式四阶段命名或外观标准。
- **季节与天气**：温带和山地使用春夏秋冬、晴/雨；热带和极地允许依其真实气候改为雨季/旱季、融冻/极夜等同等可读的状态，但必须在资产清单中显式列出，不以运行时随意改色替代。
- **地图放置**：每座城市只占一个节点锚点；远景、中景、近景都以同一张阶段城市贴图的不同显示尺度呈现，旗帜给出名称，规模圆环表达体量。城市内部工程不逐栋摆放，避免路径、碰撞、遮挡和数百贴图常驻的问题。

## 工程贴图分级与施工序列（R5 内容门完成后执行）

只有改变地表轮廓、天际线或地图战略识别的工程进入“独立贴图工程”清单；普通城市工程通过城市阶段、网络线和指标变化表现。水坝、港口、巨型矿场、轨道枢纽、大学城级科研区等候选项目须逐项判定，不能按名称批量套图。

每个获选工程在清单中明确所需画面：

```text
施工：地基 → 主体过半 → 收尾调试 → 完工
投用：完工常态 + 四季变体（必要时再增加雨雪/洪水等气象变体）
```

水坝、港口等有明确地貌前提的工程，直接由河谷、峡口、海岸/深水港等地图属性限定可建位置；不再为每一种随机地形重复生成无意义的版本。

R5 完成后、任何大批量绘制开始前，必须交付并冻结一份“地图资产清单”，至少逐项列出：工程/城市类别、适用地形、发展或施工状态、季节/天气变体、单图尺寸预算、总图片数、地图锚点规则和触发条件。该清单由 Codex 编写与审阅，DeepSeek 不参与美术选择或玩家文字。R8 已以 [R8_TEXTURE_OPTIMIZATION_BIBLE.md](R8_TEXTURE_OPTIMIZATION_BIBLE.md) 和 `content/r8/texture-production-manifest.json` 完成首次冻结；后续批次不得脱离该协议单独改风格。

## 聚居地更新原则

一般工程不会逐一生成建筑图。它们累计到人口、供给、工业、教育与行政阈值后，驱动聚居地阶段更新。中远景永远将它读作一个地点，而不是一组工程名；规模圆环的面积代表其体量。医院、大学、科研所、大发电设施等改变城市天际线或社会功能的地标才可能获得独立贴图。

当前河谷原型的阶段判定先只绑定已经完成的实体工程，以验证这条链：净水干线 → 定居点；净水干线 + 温室或工务所 → 河谷工务镇；四项首期工程全部完成 → 河谷城市。后续内容阶段再加入人口和行政等级门槛。
