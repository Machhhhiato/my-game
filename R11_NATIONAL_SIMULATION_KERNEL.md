# R11 国家模拟内核：数据变化骨架与算法

> 状态：冻结中的通用内核。R11 不替换 R9 自然骨架或 R10 文字试玩；它规定二者未来如何通过稳定 ID、作用域、操作与效果接入同一份战役状态。

## 1. 目标与边界

R11 的目标是让聚居地、城市、都市圈、国家、外部势力、舰队、轨道资产都能使用同一套数据语言持续变化。

- 不做逐人、逐格或金融市场模拟。
- 不让城市、地区或地方机构成为独立主权势力；它们是玩家国家的内部作用域。
- 不把科技、工程、政策、外交和军事行动混成一个无意义的“加点按钮”。它们共享执行外壳，但有不同的结果类型。
- 不让 UI、显示名、地图贴图或测试夹具成为模拟事实来源。

## 2. 四层结构

```text
静态定义：QuantityDefinition / TechnologyDefinition / ProjectDefinition / PolicyDefinition / PolityTemplate
      ↓
动态事实：Polity / City / MetroArea / Facility / Network / Relation / Capability
      ↓
执行过程：Operation（投入、人力、进度、里程碑、风险、效果）
      ↓
账本与观察：DailyLedger / Report / Observation（玩家知道的版本）
```

存档只保存动态事实、正在执行的操作、历史与观察；静态目录按 ID 从内容包读取。

## 3. 通用数据语法

所有长期对象都带有：稳定 ID、作用域、所有/运营/维护/指挥责任、生命周期、来源和可见性。

所有数值都要引用 `QuantityDefinition`，后者规定单位、显示模式、允许的聚合方式与精度。数值可为：精确计数、覆盖天数、标准日供给单位、能力、服务覆盖、比例、状态等级；不允许把所有数据压成 0–100。

所有变化只经 `EffectSpec` 写入。效果有目标、操作、时机、来源和原因键；每日账本记录真实变化，UI 只读取账本和状态的投影。

工程完成可使用 `facility` 或 `network` 效果写入实体事实：设施带宿主城市、权责、生命周期、维护编制和持续效果；网络带端点、容量、冗余和生命周期。它们不是一次性“工程完成 + 点数”，而是之后每日结算的来源。操作还可声明能力、设施、网络或已完成操作前置；启动时统一检查，失败不得扣除投入。

## 4. 每日算法

```text
1. 推进日历，清理过期观察与临时状态
2. 结算正在运行的设施：服务输出、维护需求、劣化风险
3. 结算活跃操作：检查编制、消耗投入、推进工作或期限、触发里程碑
4. 应用完成/持续效果：能力、数量、设施状态、关系、解锁
5. 重建派生值：可调度人手、都市圈人口与共同服务、国家能力投影
6. 检查短缺、维护积压、环境、外交、战争和阶段门条件
7. 只写入重要账本与报告；同种子、同状态、同指令序列必须得到相同结果
```

日是底层最小步长；旬、月、年只是聚合报告与一部分慢变量的检查窗口。

## 5. 多势力与精度

所有独立主权势力使用共同 `PolityState` 脊柱，并按能力开启城市、军事、外交或航天模块。玩家国家内部地区不是势力。

`dormant → strategic → regional → active → full` 决定一个势力是按年汇总、按区域结算，还是完整逐日结算。`ObservationState` 与真实 `PolityState` 分离，玩家只能看到其情报所允许的版本。

## 6. R10 接入规则

R10 的日历、人力、储备、建设物资、研究、工程、政策、失败状态和每日账本将逐步映射到 R11：

- `TextIdleState.reserves` → 共同体作用域的 `QuantityValue`；
- `workforce` → `PolityState.workforce`；
- `research/project/policy` → `OperationState`；
- 完成项目 → `FacilityState` 或 `CapabilityState`；
- R5 `runtime` → R11 `OperationSpec` 与 `EffectSpec` 的内容来源。

本轮只建立并测试内核，不迁移 R10 存档、不接 UI、不批量重写 R5 内容。

## 7. 验收

1. 作用域、单位、所有权、生命周期、操作、效果、观察和历史均有明确类型。
2. 纯模拟能够让设施每日提供效果、让操作消耗投入/占用人力/完成后应用效果、让过期政策停止生效。
3. 都市圈能由多个城市重建人口汇总；关系与玩家情报不混写。
4. 双势力、双城市的通用夹具同输入确定性通过；R9/R10/UI 不被引用或改写。

## 8. 量纲目录与阶段展示

`R11_QUANTITY_CATALOG` 是全时代可引用的单位目录。每条量纲规定：内部单位、允许范围、精度、事实是否存储、展示面板、最早可见阶段与文案键。

| 阶段 | 首先开放的量纲 | 不应提前显示的量纲 |
|---|---|---|
| 存续共同体 | 水/食物/维修覆盖天数、LDU 建设物资、工务/研究、协作/环境 | NDU、军队战备、外交信任、轨道能力 |
| 聚居地 | 能源、医疗/供水服务覆盖、维护积压 | 国家统筹、舰队、外交、航天 |
| 区域 | 统筹、运输、防卫、教育、外交与情报 | NDU、发射与星际通信 |
| 统一国家 | NDU、完整国家能力、城市群和区域执行 | 航天能力，除非已经具备实体前置 |
| 航天/星际 | 发射吨位、航线补给、通信延迟、轨道资产 | 无 |

顶栏只读取标记为 `topbar` 且已达到 `visibleFrom` 的量纲；精确舰艇/设施数量在军事、城市或航天对象详情读取，不能被压成通用点数。

## 9. 开发存档夹具

R11 提供三份无剧情、可重建的时间点存档夹具，不是正式战役或世界观事实：

| 夹具 | 用途 | 必须覆盖 |
|---|---|---|
| `save.fixture.early-community` | 验证起步生存与最小 UI 投影 | 共同体、储备、人手、初级工程/研究、有限接触 |
| `save.fixture.unified-nation` | 验证国家和都市圈数据 | 单一主权、多地区、多城市、都市圈、NDU、区域执行；无内部独立势力 |
| `save.fixture.diplomatic-conflict` | 验证外部关系与冲突 | 两个独立主权势力、情报、协定争议、精确舰艇数量、战区、补给与战备 |

三个夹具都使用同一套 `NationKernelState`，并通过结构校验与确定性模拟；它们只为后续调试服务，不能被当作默认剧情或正式存档。

## 10. 开发检查器

开发期可使用 `?kernel=early`、`?kernel=unified` 或 `?kernel=conflict` 打开 R11 国家状态检查器。它只在内存中装载对应夹具，允许推进 1 日或 10 日，以核验量纲开放、城市/都市圈归属、外部势力、舰队、战区与因果账本；不会读写 R10 正式试玩状态，也不是玩家界面。
