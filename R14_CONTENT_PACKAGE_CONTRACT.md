# R14 内容包契约：从开发夹具到可复用中后期内容

> 状态：开发内核已实现。此文件规定中后期科技、工程、政策、军事与外交内容如何挂入任意合格战役，而不是复制一份带固定城市名的存档。

## 1. 要解决的问题

R12 与 R13 已验证国家服务、舰队与外交的因果链，但验证夹具仍需要把内容接到一组具体的城市、港口与舰队 ID 上。若直接照此扩写，新的世界、城市布局或对手都会迫使内容复制，最终回到剧本特判。

R14 将二者分开：

```text
静态内容包（科技/工程/政策定义与玩家文字）
        +
角色绑定（本局哪个实体承担工业核心、港口、舰队、关系）
        ↓ 安装前校验
动态操作状态（进度、人力、成本、效果、账本）
        ↓
该局国家与世界事实
```

## 2. 三类数据，不能混写

| 层 | 内容 | 允许包含 | 禁止包含 |
|---|---|---|---|
| 内容包 | 行动定义、前置、标准效果、玩家文字、需要的角色 | 稳定内容 ID、能力/模块/设施 ID、角色名 | 固定城市 ID、坐标、地图贴图、河谷故事名、某局数值进度 |
| 角色绑定 | 本局实体承担的功能位置 | `playerPolity`、`portCity`、`playerSeaFleet` 等 → 实体 ID | 玩家可见剧情文字、结算逻辑、视觉资产 |
| 动态状态 | 已开工/完成、工时、成本、人力、设施状态、关系变化 | 本局日期、实际实体 ID、因果账本 | 对另一局存档的引用 |

内容包不是存档；安装内容包不应覆盖既有行动。重复 ID 或错误实体类型必须明确报错。

## 3. 当前角色与适用条件

### 统一国家公共服务包

`package.unified-nation.core-services` 需要：中央政府、工业核心城市、港口城市、行政核心城市、公共服务城市、物流卫星城市。它提供 10 项研究、10 项工程、10 项有限期限政策。

### 区域外交冲突包

`package.regional-diplomatic-conflict.maritime` 需要：中央政府、相邻独立势力、港口城市、行政核心城市、己方海上舰队和双边关系记录。它提供科研、工程、军事行动、港口政策与外交行动。

角色是**功能条件**，不是地理名称：内陆世界可以把具备船坞条件的湖港绑定为 `portCity`；不同世界可以把另一座行政城市绑定为 `administrativeCoreCity`。

## 4. 当前实现与下一步迁移

`src/v2/nationKernel/contentPackage.ts` 已提供：

- `NationContentPackage`：静态包的标准外壳；
- `ContentRoleRequirement`：角色名称、实体类型与说明；
- `installContentPackage`：安装前验证角色存在、类型正确，并阻止行动 ID 静默重写。

R12/R13 已通过该契约安装到夹具。为避免在回归过程中一次性重写 40 条已验证效果，现有定义内部暂保留“旧字段 → 通用角色”的适配层。这是技术迁移层，不是新的游戏数据格式。

下一批批量内容进入前，应将效果也转成纯声明式模板（例如“在 `portCity` 创建设施”），由通用编译器解析角色。届时可移除适配层，不能把适配层复制到千级目录。

## 5. 验收口径

`scripts/r14-content-package-sim.ts` 必须证明：

1. 同一内容包换成完全不同的实体 ID 后，操作效果只引用新绑定；
2. 固定夹具 ID 不会从内容包泄漏进运行结果；
3. 缺失角色、角色类型错误与重复操作 ID 都会被拒绝；
4. R12、R13 原有因果回归仍通过。

这仍是底层开发验证；R10 文字挂机、正式战役模板和地图落位尚未接入本包。
