# R8 贴图优化：统一生产基准与接入协议

## 本阶段的结论

星球不应只是一张噪声染色图，也不应变成由大量图标拼成的棋盘。正式表现采用两层：**可存档的地理底层说明“这里是什么环境”**，**透明底像素贴图说明“在这里能一眼认出什么”**。

目标是“放大后的环世界式地图可读性”：从中近景就能看出连贯山脊、林簇、河谷耕地、海岸崖地和逐渐成长的聚居地；不是现实卫星图，也不是飘在地面上的抽象图标。

本文件冻结所有贴图的共用规则。具体文件、数量、尺寸、路径与批次见 [texture-production-manifest.json](content/r8/texture-production-manifest.json)。任何一批绘制或生成都必须先满足本文件，再进入游戏。

## 地图表现层

| 层 | 事实来源 | 表现 | 什么时候出现 |
|---|---|---|---|
| 地理底层 | 世界种子、海拔、气候、流域、地貌模块 | 连续色阶、阴影、水面、河网、道路/管线 | 始终可见 |
| 地貌贴图层 | 已保存的模块类型、坡向与确定性散布种子 | 透明山脊、岩层、林簇、沙丘、农田、岸线贴图 | 中近景；越近密度越高 |
| 聚居地层 | 气候/地貌型 + 聚居地发展阶段 | 一张通用城市轮廓贴图 + 名称旗帜 + 规模圆环 | 全缩放层级 |
| 地标层 | 工程状态 + 合法地形锚点 | 施工/投用透明贴图；远景退回旗帜与规模圆环 | 中近景 |

地貌贴图不能决定地理；它只能放大已经存在的山、谷、森林和海岸。道路、水网、污染、工程施工和城市成长仍由存档状态驱动。

## 统一风格规范

- **透明背景是硬要求。** 不留黑底、纯色底、投影矩形，也不通过混合模式假装透明。
- 像素密度为“远看读轮廓，放大能读结构”。常规地貌贴图 48×48 或 64×64，城市 64×64，工程施工 48×48、投用 64×64；单文件最高 20KB。
- 使用低饱和岩灰、土褐、苔绿、冷水蓝、浅雪白与少量琥珀灯火。外轮廓为深蓝绿一像素，亮面不超过局部面积的 15%。
- 禁止照片纹理、渐变大光晕、霓虹边框、文字、数字、徽章，以及任何固定剧情地点元素（例如特定河流、`第 07 号`、河谷名称）。
- 同一物体在不同状态只改变**建造完整度、灯火、烟尘、积雪/雨水**，不更换透视角度、主轮廓或色板。
- 渲染端一律关闭平滑缩放；图片只在需要时加载，远景不显示每一项工程。

## 完整资产范围

### 1. 地貌库：24 张

覆盖山地、高地、河谷、平原、森林、干旱地、寒带、海岸八个家族。山地库尤其包括：山脊顶线、孤峰、肩坡、雪冠；它们会依坡向串成山脉，而不是反复摆放同一座山。

每种贴图由地貌模块控制位置和密度：山地沿高程脊线，高地沿断崖，河谷沿水系与耕地，森林按湿度斑块，海岸按湾、滩、岬角。海洋不使用静态贴图铺底，仍由底层绘制，避免海面花纹重复。

### 2. 通用聚居地库：20 张

采用五类气候地貌（温带内陆、干旱内陆、热带丛林、高地山地、极地寒带）× 四个内部发展阶段。阶段键仅用于资产选择，暂不作为玩家最终时代命名：`stage_1`、`stage_2`、`stage_3`、`stage_4`。

城市贴图只描述轮廓、道路聚合、屋顶密度和天际线，不画某条河、具体建筑名称或地方标识。城市内普通工程通过发展阶段和网络痕迹吸收；医院、大学、研究设施等只有在达到“独立地标”标准后才另出图。

季节和天气不是首批强制资产。当前运行时没有稳定的季节/天气事实层，先不制作会被闲置的数百变体；该层完成后，可在同一清单中追加透明积雪、雨湿和雨季版本，而不改动基础键。

### 3. 可复用工程地标库：320 张

R5 已把 1,000 个工程压缩为 40 个可复用设施家族，每个家族 8 张状态图：地基、主体、收尾调试、投用，以及晴、雨、雪、尘四种投用状态。共 320 张，不为每个工程重复出图。

当前可玩切片先接入最常出现的 4 个家族（净水连接、基础食物、基础工业、物流互联）共 32 张；其余 288 张仍是同一套风格、同一轮审校下的第二波生产，不允许各自另定画风。

## 批次与验收

“一起出”指先把**全部 364 张需求和同一视觉基准冻结**，然后按对象类别制作、每波在同一色板和 QA 下验收；不意味着把 364 个不同对象塞进一张生成请求。推荐顺序：

1. `terrain-library`：24 张地貌贴图，优先解决现在“地貌方向对但看不出来”的问题。
2. `settlement-library`：20 张通用城市阶段图，与地貌库共同接入地图。
3. `runtime-wave-1`：首期 4 个工程家族的 32 张施工/投用图。
4. `landmark-wave-2`：剩余 288 张工程图，按 40 家族清单完成。

每波验收的硬门：路径和键与清单一致；100% 真透明；≤20KB；在深色、浅色、雪地和沙地底图上无矩形底；缩小至 0.5 倍仍能辨认轮廓；放大至 2 倍不出现模糊重采样；不含任何河谷专有元素。

## 代码接入协议

- 运行时只以清单 `id` 和保存的地貌/工程事实选择资源，不根据中文名称猜测图片。
- `terrain.ts` 继续提供连续基础色与地理关系；`planetMap.ts` 只在对应缩放阈值加载地貌贴图层。
- 聚居地：`climate × stage → settlement asset id`；旗帜绘制名称，规模圆环表达体量。
- 地标：`assetFamily × facilityStage × weather → landmark asset id`；无合法锚点或远景时退回旗帜/施工线。
- `scripts/check-map-assets.mjs` 继续是体积门；`scripts/validate-r8-texture-production-manifest.mjs` 是需求完整性门。
- DeepSeek 只能依已冻结的键接入、懒加载和测试，不能决定要画什么、怎样画，也不能创建玩家可见的美术说明。

## 尚未进入本批的内容

- 逐栋城市建筑、居民、车辆和作业动画。
- 真实季节/天气系统尚未提供事实层前的变体贴图。
- 任何未来时代独有的巨构和轨道设施；届时使用相同资产卡新增，不回填或修改本阶段通用贴图。
