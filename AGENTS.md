# Always Game 协作 AI 工作规则

## M0 四层短语启动（`context/m0-direction`）

当新任务的首条有效用户消息是 `你是第一层`、`你是第二层`、`你是第三层` 或 `你是第四层` 时，先执行项目内路由，不沿用旧聊天记忆或旧 R37/R38 待办：

1. 确认当前是真实 Always Game 仓库，分支为 `context/m0-direction`，工作区没有会妨碍接管的未提交改动。
2. 依次读取 `AI_CONTEXT/M0_DIRECTION/WORKFLOW_BOOTSTRAP.md`、`LAYER_AND_LANGUAGE_RULES.md`、`NEXT_ACTION.md`、`SESSION_ROUTING.md`、`ACTIVE_TASKS.md`、对应 `roles/` 角色卡、`PLAIN_LANGUAGE_RULES.md`、`TASK_PACKAGES.md` 中的当前任务卡，以及 `NEXT_ACTION.md` 指向的最新原话证据。
3. 只返回固定启动回执；启动语只选择层级和恢复上下文，不等于开始任务，也不授权修改游戏。
4. 仓库、分支、文件、依赖或路由状态不一致时返回明确的 `BLOCKED_*`，不得自行猜测或使用旧聊天覆盖 Git。

项目内短语路由只保证从真实仓库根目录启动的新任务生效。跨任意其他项目目录的全局路由不是当前前置条件。

短语启动时，本节替代下面第 0 节的普通必读顺序；完成启动回执后，再按当前任务需要回查项目原始事实，避免旧状态文件覆盖 M0 路由。

### M0 层级硬隔离

- 第一层只负责方向、系统规则、任务拆分、线程派发、审查、退回、验收和 Git 路由。第一层可以只读检查产品文件，也可以维护 `AI_CONTEXT/M0_DIRECTION/**` 与本入口规则，但不得修改游戏代码、样式、数据、资产、测试、构建或发布文件。
- 任何产品文件写入都必须先有对应层任务卡，再由第一层创建或复用职责正确的下层 Codex 任务。线程 ID、模型、任务范围和基线必须在写入前登记；没有真实派发记录时，不得开始实现。
- `项目内持续总控`、`允许全部相关操作`或既有长执行授权，只允许第一层管理、退回、提交和推送已经由下层完成的工作，不授权第一层亲自代写第二层故事、第三层玩家文字或第四层代码。只有用户针对某一次任务明确说“允许第一层直接修改”才构成单次例外。
- 创建接口报错且安全旧线程不可复用时，返回 `BLOCKED_THREAD`；已经创建成功但能力不足时，按规则关闭并换用更合适模型接替。两种情况都不得由第一层自行补做。
- 过去由第一层直接补修 `M0-L4-401`、`401-R1`、`402-R1`、`402-R2` 的记录只保留为历史事实和流程反例，不构成后续先例。

### 下层任务产出读取二次核验门

- `read_thread`、`wait_threads`等应用读取接口返回`items: []`、`latestAssistantMessage: null`、空正文或只显示`completed`，只能登记为`API_EMPTY_READ`，不得据此认定下层没有产出、不得标记`BLOCKED_THREAD`、不得重发任务、不得新建或接替任务。
- 第一层在判定“空交付”前，必须使用准确的任务ID和回合ID检查本机对应的`$CODEX_HOME/sessions/**/rollout-*<thread-id>.jsonl`，至少核对该回合的`response_item`助手消息、`event_msg.item_completed`中的`AgentMessage`以及`event_msg.task_complete.last_agent_message`。本地任务日志存在正文时，以日志中的实际结果为准，恢复原交付并继续审查。
- 用户指出“任务已经返回”时，必须立即视为读取层不一致证据：先查本地任务日志，再修改任何任务状态。禁止在本地日志核验前继续发送“请重发”“最小输出测试”或重复创作要求。
- 只有应用接口与本地任务日志都没有目标回合的助手结果，且已核清准确任务ID、回合ID与完成状态后，才能登记`OUTPUT_MISSING_VERIFIED`；它仍不自动等同于任务能力失败。`BLOCKED_THREAD`只用于创建、路由、恢复或执行本身确实不可用的情况。
- 每次因接口漏显造成误判，都必须在当前session与决策记录中明确撤销错误状态，不得只追加新结论而保留相互冲突的活动状态。

### 沟通用语与玩家展示用语分离

- 方向讨论中的方便说法、类比、机制简称和内部字段，不自动成为按钮、标题、提示、正文或日志。`远期科技`、`一格`、`标准包 v1`、`系统自动补齐`等词可以帮助讨论，但未经第三层正式文字或用户逐字确认，不得直接显示在页面。
- 界面结构说明与玩家可见文字必须分别记录。前者写页面职责、数据来源和交互；后者写玩家最终看到的准确字符串。第四层只能接入已接受字符串，缺少时返回 `BLOCKED_TEXT`，不得把任务说明改写成页面解释段落。
- 新第一层任务除摘要外，还必须读取最新 session 或 amendment 中保存的用户原话。摘要用于导航，不能替代语气、否决过程、例子和逐步修正。

### 科研、工程与临时法令文字强制源

- 任何任务涉及科研、工程或临时法令的名称、效果、正文、故事、审核、数据录入或页面接入，必须全文读取 `AI_CONTEXT/M0_DIRECTION/RESEARCH_ENGINEERING_EDICT_TEXT_RULES_001.md`，并在开始工作前提供第 1 行至 EOF 的读取范围和实际 SHA-256。
- 科研文字任务还必须全文读取 revision 6 `AI_CONTEXT/M0_DIRECTION/references/reference-m0-tech-description-corpus-001.md`；专用规则中的摘录、任务摘要和模型记忆均不能替代原始语料。
- 漏读、截断或只读摘要时返回 `BLOCKED_SOURCE`；第四层没有 `accepted_verbatim` 字符串时返回 `BLOCKED_TEXT`。`positive_reference`、`review_candidate` 和 `rejected_example` 均不得接入正式页面或数据。
- 科研卡的第二行不是常规字段。完全严肃科技只写科技正文；只有确实采用笑话或黑色幽默并通过资格与专属因果审查时，才允许增加承担笑点或黑色后果的第二行。严肃的历史画面、生活影响或气氛收尾不得单独占据第二行。
- 玩家可见科技名称最多七个汉字，只允许汉字，不得含标点、空格、字母或数字。名称出现逗号、顿号等并列符号时，必须退回`fact_gate`检查是否混入多项科技；禁止只删符号或压缩措辞后继续使用。

### 科技审查默认使用事实层与文风层两道串行门禁

- 任何科技名称、节点、效果或正文任务，默认先执行`fact_gate`：只判断这项科技是否应当作为独立节点存在，包括身份、相邻差异、时代与产业前置、解锁真实性，以及科技与工程建成、产品生产、调查完成或制度实施之间的边界。
- `fact_gate`必须先审核名称是否只指向一项能力。超过七个汉字、含任何符号，或依靠逗号、顿号并列多个对象与动作的候选名称直接退回；不得用短标题掩盖实际仍然聚合的节点。
- 逐项审查时同步维护科技事实问题账。确定性错误继续阻塞正文；只有必须结合整棵树才能裁决的合并、下沉、重复里程碑、玩家价值或尺度疑问，才可标记`fact_gate: pass_with_tree_review`并继续暂定正文。
- `fact_gate`没有通过时，禁止生成或润色正文；不得用漂亮文字替错误节点补身份。
- 只有`fact_gate: pass`以后，第三层才能生成玩家正文，第一层再独立执行`prose_gate`：审查正文身份、能力意义、自然中文、对象与主语、句法节奏、科技专属因果、形式多样性和玩家第一次阅读是否能够理解。
- 事实正确、语法无误、效果明确或原有审查投入很多，都不能代替`prose_gate: pass`。像说明书、培训材料、审计报告、效果解释或同义重复的文字必须退回。
- 一项科技只有按`fact_gate通过 → prose_gate通过 → 用户审阅`完成当前步骤后，才可进入下一项；用户逐字接受以前不得标记`accepted_verbatim`。
- 第一层为解释问题临时写出的修正示意默认是`discarded_review_explanation`，不得自动成为正向范例、正式候选、后续生成提示或页面文字。
- 全部科技正文完成后必须执行一次覆盖整棵科技树的游戏设计复审，逐项裁决保留、合并、拆分、删除、下沉、改前置或改解锁。文字完成不等于科技树合理；正式结构修改仍须另行确认和重新审查受影响内容。

## 0. 进入项目后的必读文件

每次进入项目，任何 AI 必须先阅读：

1. `AI_COLLABORATION_RULES.md`
2. `AI_START_HERE_NEXT_SESSION.md`
3. `AI_CONTEXT.md`
4. `CURRENT_STATUS.md`
5. `DECISION_LOG.md`
6. `IDEAS_POOL.md`
7. `AI_CONTEXT/PROJECT_MEMORY.md`
8. `AI_CONTEXT/CURRENT_STATE.md`
9. `AI_CONTEXT/DECISION_LOG.md`
10. `AI_CONTEXT/WORLD_RULES.md`
11. `AI_CONTEXT/NEXT_TASK.md`

之后再按任务阅读相关的原始来源、冻结规格和实现文件。所有 GitHub 提交必须遵守 `AI_COLLABORATION_RULES.md` 的“功能与相关上下文同提交、同推送”规则。AI_CONTEXT 是导航与约束摘要；若需要具体事实，必须回查原文件，不能只凭摘要臆测。

## 1. 修改前的检查

- 修改 `LORE.md`、`TIMELINE.md`、`TECH_INTEGRATED.md`、`NOVEL_OUTLINE.md`、`CAMPAIGN_CHRONICLE.md`、内容目录、世界模板、系统规格或核心模拟前，必须检查已有设定、决策与相关阶段规格。
- 先确认该内容属于世界硬设定、通用系统、战役模板、剧本夹具还是呈现文案；不得跨层写入。
- 对大规模改动，先检查当前差异、明确影响范围和验收方法；不以旧 Demo 行为替代当前规格。

## 2. 新增内容的强制审查

任何科技、工程、政策、事件、制度、战争或时代节点，至少要考虑：

1. 技术合理性与前置能力；
2. 历史连续性与上一阶段留下的问题；
3. 产业、维护、能源、物流与环境承载；
4. 社会影响与普通人的生活变化；
5. 政治、经济、军事、权利与地方执行影响；
6. 对玩家可见的选择、代价、反馈与可追溯性。

新增系统优先先写对象、关系、状态、输入输出与验收，再批量填内容或制作画面。

## 3. 不可违反的设定与实现边界

- 玩家是单一主权中央政府的战略决策者；地方只有有限执行自治，不具备独立外交、军队、货币、战略资源或退出权。
- 游戏是持续挂机战略，不是逐人、逐格、逐建筑或战术单位微操。
- 科技必须产生“技术 → 产业/设施 → 社会 → 政治 → 人类生活”的因果链；工程必须是实体项目，政策必须是有限期行政行动。
- 世界生成、工程结果与视觉层必须分离；显示名、屏幕坐标、贴图和剧情文本不可作为通用逻辑主键。
- `第 07 号—翡翠河谷—旧渡口` 只可作为独立测试夹具或可选剧本，不得重新硬编码为通用系统或默认战役。

## 4. 严格禁止

- 随意推翻已有设定，或把未确认方案写成既定事实；
- 只增加科技等级/数值，不描述产业、社会和生活变化；
- 引入没有历史与工程基础的突然技术跃迁；
- 为追求戏剧性破坏世界逻辑、核查约束、维护成本或战争后果；
- 用测试夹具、固定地名、显示名、图片文件或屏幕坐标驱动通用规则；
- 将开发术语、内部 ID、权重、债务名或状态机直接暴露为玩家文案；
- 未经明确授权删除旧存档、扩大范围、引入依赖、提交或推送。

## 5. 文案、视觉与外包职责

- Codex/项目主 AI 负责世界观、玩法、数值逻辑、玩家文案、视觉规则、交互设计和验收判断。
- DeepSeek 或其他外包实现 AI 只可承担已冻结规格中的纯代码填充、状态接线、数据录入、校验器、构建和测试；不得自行创作或调整文本、图片、布局、颜色、数值、故事、地图内容或范围。
- 玩家文字必须按 R6 的对象分类与口吻编写。若文字、视觉或数值没有冻结的来源，实施 AI 应报告阻塞，不得自行补默认方案。

## 6. 验收与状态纪律

- 不把“文件存在”“DOM 渲染”“类型检查通过”称作玩法、美术或世界观验收。
- 任何已完成/待完成/仅规划/旧稿状态必须如实标记。
- 工程、科技、事件和地图变化必须能以稳定 ID、状态和地理引用追溯；同种子、同状态、同指令应可复现。
- 完成项必须从主选择区移入可查档案；普通通知只报告会改变玩家下一步的重点事实。
