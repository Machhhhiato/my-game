# Always Game 协作 AI 工作规则

## M0 四层短语启动（`context/m0-direction`）

当新任务的首条有效用户消息是 `你是第一层`、`你是第二层`、`你是第三层` 或 `你是第四层` 时，先执行项目内路由，不沿用旧聊天记忆或旧 R37/R38 待办：

1. 确认当前是真实 Always Game 仓库，分支为 `context/m0-direction`，工作区没有会妨碍接管的未提交改动。
2. 依次读取 `AI_CONTEXT/M0_DIRECTION/WORKFLOW_BOOTSTRAP.md`、`NEXT_ACTION.md`、`SESSION_ROUTING.md`、`ACTIVE_TASKS.md`、对应 `roles/` 角色卡、`PLAIN_LANGUAGE_RULES.md` 和 `TASK_PACKAGES.md` 中的当前任务卡。
3. 只返回固定启动回执；启动语只选择层级和恢复上下文，不等于开始任务，也不授权修改游戏。
4. 仓库、分支、文件、依赖或路由状态不一致时返回明确的 `BLOCKED_*`，不得自行猜测或使用旧聊天覆盖 Git。

项目内短语路由只保证从真实仓库根目录启动的新任务生效。跨任意其他项目目录的全局路由不是当前前置条件。

短语启动时，本节替代下面第 0 节的普通必读顺序；完成启动回执后，再按当前任务需要回查项目原始事实，避免旧状态文件覆盖 M0 路由。

## 0. 进入项目后的必读文件

每次进入项目，任何 AI 必须先阅读：

1. `AI_COLLABORATION_RULES.md`
2. `AI_START_HERE_NEXT_SESSION.md`
3. `AI_CONTEXT.md`
4. `CURRENT_STATUS.md`
5. `DECISION_LOG.md`
6. `IDEAS_POOL.md`
7. `AI_CONTEXT/PROJECT_MEMORY.md`
8. `AI_CONTEXT/CURRENT_STATE.md`
9. `AI_CONTEXT/DECISION_LOG.md`
10. `AI_CONTEXT/WORLD_RULES.md`
11. `AI_CONTEXT/NEXT_TASK.md`

之后再按任务阅读相关的原始来源、冻结规格和实现文件。所有 GitHub 提交必须遵守 `AI_COLLABORATION_RULES.md` 的“功能与相关上下文同提交、同推送”规则。AI_CONTEXT 是导航与约束摘要；若需要具体事实，必须回查原文件，不能只凭摘要臆测。

## 1. 修改前的检查

- 修改 `LORE.md`、`TIMELINE.md`、`TECH_INTEGRATED.md`、`NOVEL_OUTLINE.md`、`CAMPAIGN_CHRONICLE.md`、内容目录、世界模板、系统规格或核心模拟前，必须检查已有设定、决策与相关阶段规格。
- 先确认该内容属于世界硬设定、通用系统、战役模板、剧本夹具还是呈现文案；不得跨层写入。
- 对大规模改动，先检查当前差异、明确影响范围和验收方法；不以旧 Demo 行为替代当前规格。

## 2. 新增内容的强制审查

任何科技、工程、政策、事件、制度、战争或时代节点，至少要考虑：

1. 技术合理性与前置能力；
2. 历史连续性与上一阶段留下的问题；
3. 产业、维护、能源、物流与环境承载；
4. 社会影响与普通人的生活变化；
5. 政治、经济、军事、权利与地方执行影响；
6. 对玩家可见的选择、代价、反馈与可追溯性。

新增系统优先先写对象、关系、状态、输入输出与验收，再批量填内容或制作画面。

## 3. 不可违反的设定与实现边界

- 玩家是单一主权中央政府的战略决策者；地方只有有限执行自治，不具备独立外交、军队、货币、战略资源或退出权。
- 游戏是持续挂机战略，不是逐人、逐格、逐建筑或战术单位微操。
- 科技必须产生“技术 → 产业/设施 → 社会 → 政治 → 人类生活”的因果链；工程必须是实体项目，政策必须是有限期行政行动。
- 世界生成、工程结果与视觉层必须分离；显示名、屏幕坐标、贴图和剧情文本不可作为通用逻辑主键。
- `第 07 号—翡翠河谷—旧渡口` 只可作为独立测试夹具或可选剧本，不得重新硬编码为通用系统或默认战役。

## 4. 严格禁止

- 随意推翻已有设定，或把未确认方案写成既定事实；
- 只增加科技等级/数值，不描述产业、社会和生活变化；
- 引入没有历史与工程基础的突然技术跃迁；
- 为追求戏剧性破坏世界逻辑、核查约束、维护成本或战争后果；
- 用测试夹具、固定地名、显示名、图片文件或屏幕坐标驱动通用规则；
- 将开发术语、内部 ID、权重、债务名或状态机直接暴露为玩家文案；
- 未经明确授权删除旧存档、扩大范围、引入依赖、提交或推送。

## 5. 文案、视觉与外包职责

- Codex/项目主 AI 负责世界观、玩法、数值逻辑、玩家文案、视觉规则、交互设计和验收判断。
- DeepSeek 或其他外包实现 AI 只可承担已冻结规格中的纯代码填充、状态接线、数据录入、校验器、构建和测试；不得自行创作或调整文本、图片、布局、颜色、数值、故事、地图内容或范围。
- 玩家文字必须按 R6 的对象分类与口吻编写。若文字、视觉或数值没有冻结的来源，实施 AI 应报告阻塞，不得自行补默认方案。

## 6. 验收与状态纪律

- 不把“文件存在”“DOM 渲染”“类型检查通过”称作玩法、美术或世界观验收。
- 任何已完成/待完成/仅规划/旧稿状态必须如实标记。
- 工程、科技、事件和地图变化必须能以稳定 ID、状态和地理引用追溯；同种子、同状态、同指令应可复现。
- 完成项必须从主选择区移入可查档案；普通通知只报告会改变玩家下一步的重点事实。
