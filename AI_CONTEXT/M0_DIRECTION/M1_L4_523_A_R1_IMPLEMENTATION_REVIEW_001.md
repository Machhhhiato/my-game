---
review_id: REVIEW-M1-L4-523-A-R1-001
task_id: M1-L4-523-A-R1
status: user_changes_requested_superseded_by_R2_plan
thread_id: 01a052f7-01b1-7bf2-bd78-2cb63d202c0f
thread_title: AG-M1｜L4-VISUAL｜523-A-R1｜MAC
thread_model: gpt-5.6-sol
thread_reasoning: high
worktree: /Users/xujiangyue/.codex/worktrees/c8b8/always game
input_commit: f786c9d050e989873f87ba36d93bbe6fdfead0c6
main_checkout_integrated: false
commit_created: false
push_performed: false
---

# M1-L4-523-A-R1 实现审查

## 一、审查结论

接替任务完成了当前真实 M0 纵向切片的战略控制改造，并通过第四层自动验证、浏览器验证和第一层独立代码复核；但用户试玩后发现科研被生产阻塞、勘测暂停不透明和基础资源软锁三项问题，因此本轮已经退回，不再保持待接受状态。

产品改动只存在于独立工作树`/Users/xujiangyue/.codex/worktrees/c8b8/always game`，尚未并入主工作区，没有提交、推送或发布。R2退回单与长工作计划见`M1_L4_523_A_R2_CHANGE_REQUEST_AND_NIGHT_PLAN_001.md`；R2通过用户试玩前，不进入`M1-L4-523-B`。

## 二、实际改动边界

第四层只修改了任务白名单内的八个文件：

- `src/m0/M0App.tsx`
- `src/m0/m0.css`
- `src/m0/types.ts`
- `src/m0/state.ts`
- `src/m0/progression.ts`
- `src/m0/simulation.ts`
- `src/m0/save.ts`
- `scripts/m0-core-sim.ts`

没有新增依赖，没有修改治理、军事、正式正文或正式位图资产。

## 三、已实现的核心结果

1. 新游戏默认以一倍速持续运行，一秒对应一个世界日；暂停后日期不变，恢复后继续自然推进。
2. 科技卡单击直接加入玩家队列；队列项右键移出；移出正在研究且尚未完成的科技时，未完成项目和进度一并删除；队列为空时系统不替玩家选择科技。
3. 生产改为固定产品行和持久工厂分配；工厂总量守恒，分配在存档后保留；材料或条件不足时生产线阻塞但不会被系统改派。
4. 工程和探索只保留是否开始、暂停、继续与先后顺序等战略决定；人数、线路、天数和工程队细节交由底层结算。
5. 地图继续常驻，科研、工程、生产等系统以地图上的悬浮面板打开；窄屏仍保留地图可见区域。
6. 存档升级为 v8；只接受严格匹配的 v7 旧存档；迁移成功后删除旧键，清档同时删除 v8 与 v7，避免旧存档再次复活。

## 四、第一层退回与修正

首次第四层任务因上下文耗尽留下未验证草稿，第一层将其固定为`REJECTED_INCOMPLETE_DRAFT`，没有作为接替基线。

接替任务首轮完成后，第一层又退回两项确定性问题：

- 新游戏仍默认暂停，与“玩家不操作也持续逐日运行”冲突；
- v7 旧存档迁移或清档后没有删除旧键，可能在 v8 缺失时重新出现。

接替任务已修正这两项并补充回归测试。第一层复查确认`createInitialM0State()`默认`running: true`，迁移成功和清档都会删除 v7 键。

## 五、验证证据

第四层报告以下检查全部通过：

- TypeScript 类型检查；
- M0 核心模拟测试；
- 产品构建，共 38 个模块；
- 差异格式检查；
- 1920×1080、1366×768、390×844 三种尺寸浏览器检查；
- 科技入队与右键移出、保存刷新、工厂分配和重载、工程、探索、地图常驻；
- 浏览器警告与错误均为 0。

第一层随后独立运行八文件差异检查和 M0 核心模拟。核心模拟再次通过，并覆盖一秒一日、纯玩家科研生命周期、战略探索控制、工厂守恒、严格 v8 迁移、确定性存档和资源结算。

## 六、当前边界

本轮只完成 M0 纵向切片的底层交互基准，不代表完整 M1 Demo 已经完成。治理、军事、完整科技工业内容、工程阶段贴图、地图部队和终局规模反馈仍属于后续批次。

用户已经完成本轮试玩并明确退回。下一步按照`M1_L4_523_A_R2_CHANGE_REQUEST_AND_NIGHT_PLAN_001.md`修正三项问题；当前计划已就绪但尚未启动。R2再次通过用户试玩前，不得安排并入主工作区或拆分`M1-L4-523-B`。
