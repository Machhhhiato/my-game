---
template_id: NIGHT-WORK-ORDER-M0-001
template_status: accepted
acceptance_source: M0-S003-U089
project_label: always game
real_repo: /Users/xujiangyue/AGENT Project/always game
---

# 夜间执行单模板

每个夜间代码批次开始前复制并填写一份。空白项、未知项或授权来源缺失时，不得开始代码工作。

## 任务身份

- 夜间批次编号：
- 所属正式任务编号：
- 唯一目标：
- 明确非目标：
- 预期产物：
- 用户授权消息编号：

## 项目与起点

- Codex 项目：`always game`
- 真实仓库：`/Users/xujiangyue/AGENT Project/always game`
- Host：Mac local
- 分支：
- 输入提交：
- 开始时工作区状态：
- 回退锚点：

项目归属、真实仓库或输入提交任一不符时，停止并返回 `BLOCKED_ROUTING`，不得退回 `xujiangyue` 项目继续。

## 范围

- 允许修改的文件或模块：
- 禁止触碰的文件、系统和行为：
- 必须读取的权威文件：
- 允许的外部写入：本地提交 / push / merge / deploy / 其他；逐项填写，未填写即不允许。

## 时间与检查点

- 初始预计耗时：通常约 6 小时，可按真实工作量填写：
- 首次继续判断点：通常约第 5 小时：
- 延长条件：仍属同一授权目标，且额度、上下文、环境和剩余工作都允许完整收尾：
- 当前预计收尾时段：预计结束前约 1 小时：
- 阶段检查点：至少列出一个；连续约 2 小时没有检查点时优先收束：

预计时长不是硬上限。满足延长条件时可运行 8 小时或更久；不得因此跨任务、扩范围或跳过新的权限门。

## 基线与验收

- 修改前基线行为：
- 基线检查命令与结果：
- 完成标准：
- 验收命令：
- 需要的回归测试：
- 界面或产物证据：

## 立即停止条件

- 分支、提交、项目归属或工作区状态与本单不符；
- 需要越出写入白名单、作出新设计决定、增加权限或执行破坏性操作；
- 与本任务无关的基线失败导致结果无法判断；
- 同一根因连续修正两次仍失败；
- 剩余额度、上下文或运行条件不足以同时完成修改、验证和交接；
- 出现必须由用户处理的登录、密钥或外部授权。

## 次日交接

- `night_run_status`：
- 实际耗时与停止原因：
- 完成内容：
- 未完成内容：
- 修改文件与差异摘要：
- 输入提交、输出提交与回退锚点：
- 测试通过数、失败数与未运行项：
- 本地提交和远端推送状态：
- 已知问题与阻塞：
- 需要用户决定的唯一下一步：

夜间任务不得自行把正式任务标成 accepted 或 verified，也不得自动开始下一个正式任务。
