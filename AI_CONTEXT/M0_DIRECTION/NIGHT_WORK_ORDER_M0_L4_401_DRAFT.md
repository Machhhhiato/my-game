---
work_order_id: NIGHT-M0-L4-401-001
status: accepted_authorized_dispatch_pending
formal_task: M0-L4-401
project_label: always game
real_repo: /Users/xujiangyue/AGENT Project/always game
branch: context/m0-direction
planned_model: gpt-5.6-terra
initial_reasoning_effort: medium
accepted_source: M0-S003-U126
accepted_at: 2026-08-25T23:01:41+08:00
implementation_authorized: true
commit_authorized: true
push_authorized: true
deploy_authorized: false
secondary_approval_required: false
new_thread_attempt_limit: 1
fallback_thread: 01a03800-3d9f-7c70-b1dc-70a30bc8bf01
---

# NIGHT-M0-L4-401-001 · 第一次 M0 长执行草案

用户已一次性接受本执行单，并授权创建第四层线程、修改限定范围内代码、运行测试和本地启动检查、本地提交及推送 `context/m0-direction`。执行阶段不再逐项请求审批。

## 1. 这次长工作只交付什么

建立 M0 能独立运行的第一块代码底座：时间推进、28 人汇总调度、六种库存账本、库存底线、P0–P3 供应顺序、项目自动暂停与恢复，以及完全独立的新存档。

完成后默认入口应能启动一个新的 M0 存档，并在简洁的功能界面中看到：当前日、暂停状态、人口与岗位、六种库存、当日流入消耗、底线、维护积压、队列和暂停原因。开局真实工单只接入`总部水务恢复`，用于验证玩家调整供水档位后，库存净变化、人力和修复进度同步改变。该界面只用于验证玩法底座，不作为最终 UI 或美术验收。

## 2. 单次确认包含的实施范围

### 2.1 新建独立 M0 代码

优先新建 `src/m0/**`，至少覆盖：

- 版本化 M0 状态与稳定 ID；
- 第 1 日初始状态；
- 确定性的单日结算；
- 人口汇总状态与缺水、缺粮债；
- 日常线和发展项目的人力档位；
- 六种库存、位置、锁定、预留、消耗和流水；
- 水、食物、普通零件的硬底线与玩家安全线；
- P0–P3 队列、暂停原因、人员去向与自动恢复；
- `always-game-m0-v1` 独立存档封装；
- 最小 M0 store 与功能验证界面。

### 2.2 接入新的默认入口

- 修改 `src/main.tsx`，让默认启动进入 `M0App`。
- M0 不读取、迁移、清洗、覆盖或删除任何旧存档键。
- 旧 R38、V2、NationKernel 与旧 UI 本批不删除，只是不再作为默认入口。

### 2.3 固化首轮已接受数据

- 开局人口 28；基本值守 7，供水 4，食物 5，维护与手工作坊 3，总部短途物流 3，可调度 6。
- 六库存开局：水 280、食物 560、普通零件 45、工程构件 80、合金料 40、精密部件 0。
- 容量：水 420、食物 840、普通零件 90、工程构件 120、合金料 60、精密部件 24。
- 开局主线锁定：普通零件 20、工程构件 72、合金料 32；锁定不等于已经消耗。
- 日常线最低/标准/加速人数及 T02 已接受的流入、消耗、维护能力与物流能力。
- 水、食物和普通零件的硬底线/默认安全线分别为 2/5 日、3/7 日、3/5 日。
- 一个游戏日的验证参考速度为现实 20 秒；退出后不离线推进。

### 2.4 建立自动验证链

新增 M0 专用脚本和 `package.json` 命令，至少验证：

1. 相同初始状态与指令得到完全相同的日结算结果；
2. 标准第 1 日结算为水 280、食物 562、普通零件 45，水务修复 `3/24`，旧件余量 237；
3. 所有人力去向总和始终等于当前可工作人口，不能出现幽灵工人或后台免费调岗；
4. 日常线不能低于最低值守；调整档位会真实改变流入、维护和可调度人力；
5. 安全线触发 P3、P2、P1 的逐级暂停，硬底线只保留 P0 与直接恢复工作；
6. 自动恢复必须满足安全线加两日、未来两日预测、下一个清晨和预授权四个条件，并按 P1→P2→P3 逐项恢复；
7. 缺水、缺粮债会改变可工作人口，人口变化后需求和基本值守重算；
8. 保存、退出、重载后下一日结果与不中断运行一致；
9. M0 只使用新存档键，不读取旧键；
10. 生产状态只接入`总部水务恢复`；其他 P1–P3 项目只以 `TEST_ONLY` 夹具验证队列，不提前生成科研、地图或工程内容；
11. `npm run typecheck`、`npm run build` 与本地默认入口启动检查通过。

## 3. 明确不做

- 不进入 `M0-L4-402`：不做六边球体地图、选址、科研树、精密工坊或勘测无人机。
- 不进入 `M0-L4-403`：不做首座工厂、运输批次、工程包或第二座复制。
- 不进入 `M0-L4-404`：不接完整故事、随机事故、90 日完整试玩或最终内容。
- 不设计最终 UI、颜色、动效、声音或美术。
- 不安装或升级依赖，不引入 Skill、MCP、插件或常驻服务。
- 不删除旧代码、旧资源或旧存档；本批只建立新入口并隔离旧入口。
- 不兼容旧存档，不修改 `main` 分支，不合并、不部署、不发布。

## 4. 允许修改的范围

- 新建：`src/m0/**`、`scripts/m0-*.ts`。
- 定向修改：`src/main.tsx`、`package.json`。
- 若构建确有需要，可定向修改 M0 入口直接依赖的类型声明；必须在交接中逐项说明。
- 记录：本执行单、夜间报告及相关 M0 路由文档。
- 任何旧模块只能只读检查；要修改或删除时立即停止并返回第一层。

## 5. 删除与旧底座处理

本次长执行不删除文件。开始时只完成与本批直接相关的函数级依赖核对：

- React/Vite/Tauri 外壳能否原样保留；
- 旧存档实现是否只作参考而不被 M0 import；
- `src/main.tsx` 切换后旧查询路由是否仍影响默认入口；
- 新 M0 代码是否意外依赖旧 R38、V2 或 NationKernel 状态。

发现旧代码阻碍 M0 时，优先在新 `src/m0/**` 内隔离实现；不得在无人值守期间扩大为旧代码大清理。实际删除留给后续独立批次。

## 6. 模型、上下文和时长

- 使用新的第四层执行线程，避免继承当前长聊天；第一层保留为总控线程。
- 第一层先把本执行单、授权消息和当前方向记录提交推送，核对干净输入提交，再创建第四层线程；不再请求第二次确认。
- 新建任务只尝试一次；创建失败后立即复用既有第四层代码边界线程 `01a03800-3d9f-7c70-b1dc-70a30bc8bf01`，不重复消耗额度。
- 模型：`gpt-5.6-terra`；用户考虑 Spark 额度后主动指定 Terra / medium。
- 初始思考深度：`medium`；机械检查降为 `low`。
- 最高自动升级：`high`，仅在同一范围出现复杂架构冲突或较低档明确失败时使用并记录原因。
- 禁止自动使用 `xhigh`、`max`、`ultra`。
- 初始工作量按约 6–8 小时估算；若同一目标尚可完整收尾，可以继续更久；提前完成则提前停止。
- 连续约两小时没有可验证检查点时，优先收束当前阶段，不靠扩大范围消耗时间。

## 7. 长执行内部阶段

1. **预检与基线**：核对项目、分支、输入提交、工作区、依赖、现有构建和 M0 允许范围。
2. **状态与结算**：建立状态、初始值、日推进、资源流水、人力守恒和人口缺口债。
3. **调度与安全线**：实现 P0–P3、锁定预留、预警、暂停、人员去向和自动恢复。
4. **存档与入口**：建立新存档键、重载校验、M0 store、默认入口和最小功能界面。
5. **自动验证**：完成 M0 专用模拟、类型检查、生产构建、关键存档回归和默认入口本地启动检查。
6. **收尾**：复核差异、形成可恢复本地提交、按授权推送、输出次日验收报告。

阶段之间不再向用户逐项确认。只要仍在本单范围，第四层自行修正并继续。

## 8. 立即停止条件

- 真实仓库、分支、输入提交或工作区与执行单不符；
- 必须修改白名单外旧模块、删除文件、增加依赖或作出新的玩法设计；
- 同一根因连续修正两次仍失败；
- 基线存在与本任务无关的失败，导致无法判断新代码是否正确；
- 额度、上下文或环境不足以同时完成修改、测试和交接；
- GitHub 登录、密钥或其他外部授权失效。

停止时保留已验证检查点，写明阻塞，不把半成品标为完成，也不自动请求临时授权。

## 9. 次日交付

一次性交付：

- `night_run_status` 与实际耗时；
- 完成、未完成和停止原因；
- 修改文件与架构摘要；
- 每个检查点提交与最终回退锚点；
- 自动测试、类型检查和构建结果；
- 新档、保存、退出和重载证据；
- 旧存档键未被读取的证据；
- 本地提交与 GitHub 推送状态；
- 已知问题；
- 唯一下一步。

第四层只能报告完成或阻塞，不能自行把 `M0-L4-401` 标为 accepted/verified，也不能开始 `M0-L4-402`。

## 10. 已完成的一次性确认

用户已在 `M0-S003-U126` 接受本方案，并明确授权第一层在项目完成前总控相关任务、审阅、退回、线程创建、代码修改、测试、提交和推送，不再逐项审批。

本次授权仍受执行单的正式任务范围约束：不进入 `M0-L4-402`，不删除旧文件，不安装依赖，不合并、部署或发布。
