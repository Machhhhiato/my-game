---
night_run_id: NIGHT-M0-L4-401-001
formal_task: M0-L4-401
night_run_status: completed_waiting_user_acceptance
input_commit: 2820297d55cbad46d4b525f74520da2271e022eb
output_commit: 2a871c08cf035877cab60301f63f6c558ad32cf5
handoff_sync_commit: this_commit
branch: context/m0-direction
reasoning_effort: medium
highest_reasoning_effort: medium
reasoning_upgrade: none
started_at: 2026-08-25T23:01:00+08:00
stopped_at: 2026-08-26T00:01:00+08:00
elapsed: approximately_60_minutes
---

# NIGHT-M0-L4-401-001 · 执行报告

## 状态

`completed_waiting_user_acceptance`。

M0-L4-401 的代码、第一层审查、自动验证、生产构建和真实本地入口检查已经完成。它没有被标为用户 `accepted`，也没有进入 `M0-L4-402`。

## 线程与审查过程

- 按用户要求只尝试一次新建 `gpt-5.6-terra / medium` 第四层任务；创建因启动参数冲突失败，没有进行第二次新建。
- 随即复用既有第四层线程 `01a03800-3d9f-7c70-b1dc-70a30bc8bf01`。
- 第一版虽通过它自己的测试，但第一层否决：T02 流量错误、人数不足仍有产出、锁定零件可被日常维护使用、P0–P3 人员去向未落地、TEST_ONLY 混入玩家状态、存档校验过浅。
- 同一线程完成 R1 后，第一层再次独立检查并修正：状态浅拷贝污染、缺件仍免费消除维护积压、同版本损坏存档可穿过校验、内部项目 ID 和错误暂停原因泄漏、页面继承旧入口深色背景而不可读。
- 第四层线程没有提交或推送；最终代码、验收判断和 Git 收口由第一层负责。

## 已完成实现

- 新建版本化 M0 状态、稳定项目 ID、确定性单日结算和现实 20 秒/游戏日时钟；开局默认暂停，退出后不离线推进。
- 接入 28 人开局、正常/无法工作/危急/死亡汇总、缺水债与缺粮债、基本值守重算和岗位不足反馈。
- 接入四条日常线的最低/标准/加速档；正常档位变更先核对完整编制，不能静默截断后续岗位。
- 接入六种总部库存的容量、地点、锁定、预留、累计消耗和逐日流水；自由库存计算不使用锁定或预留物资。
- 固化 T02 供水、食物、维护、旧件修复和总部物流数值；人数低于最低档时不产生幽灵产出。
- 缺少普通零件时，未完成的正常维护会形成真实积压；库满时停止多余修复，不丢弃可修旧件。
- 接入已批准的低效总部拆解：只有 5 名维护人员、旧件耗尽且普通零件下一日会低于安全线时启动，净增 1 个普通零件，并记录被拆除的总部可用物件。
- 接入水、食物、普通零件的硬底线和可调整安全线；玩家安全线不能低于硬底线。
- 接入 P0–P3 暂停、岗位回流、原地待命、等待确认和自动恢复；恢复同时检查安全线加两日、未来两日预测、清晨日结算、预授权，并按 P1→P2→P3 一次一个、稳定一日后继续。
- 正常生产状态只包含`总部水务恢复`；P1–P3 额外对象只由测试注入，玩家界面不显示 TEST_ONLY。
- 新存档只使用 `always-game-m0-v1`；损坏 JSON、残缺对象、非法档位、损坏项目或账本会安全回到新档，绝不读取旧键。
- 默认入口切换到 `M0App`；旧 R38、V2、NationKernel、旧资源和旧存档没有删除或修改。

## 修改范围

```text
package.json
src/main.tsx
src/m0/types.ts
src/m0/state.ts
src/m0/simulation.ts
src/m0/save.ts
src/m0/M0App.tsx
src/m0/m0.css
scripts/m0-core-sim.ts
AI_CONTEXT/M0_DIRECTION/NIGHT_RUN_M0_L4_401_001.md
AI_CONTEXT/M0_DIRECTION 下相关路由与审计记录
```

没有修改白名单外旧模块，没有删除文件，没有增加或升级依赖，没有部署、合并或发布。

## 验证证据

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 通过。 |
| `npm run test:m0` | 通过。覆盖 T02、锁定/预留、容量、人数不足、状态不可变、P3→P2→P1 暂停、P1→P2→P3 恢复、人员守恒、低效拆解、人口债、存档结构、旧键隔离和时间。 |
| `npm run build` | 通过；Vite 生产构建 34 个模块。 |
| `git diff --check` | 通过。 |
| 默认入口启动 | 受限环境内首次 `listen EPERM`；按权限流程在本机启动后通过，Vite 返回 `http://127.0.0.1:5173/`。 |
| HTTP 烟雾检查 | 通过；绕过本机代理访问 `127.0.0.1` 后返回默认 HTML 与 `/src/main.tsx`。 |
| 浏览器渲染 | 通过；初始人口、四档调度、六库存、底线、队列和流水均可见，无应用控制台错误。 |
| 浏览器交互 | 通过；标准第 1 日为水 280、食物 562、普通零件 45、水务 3/24；供水加速后人力仍为 28/28，下一日水务增至 9/24；安全线低于硬底线被拒绝。 |
| 真实时间推进 | 通过；点击开始后约 20 秒只推进 1 日，并可恢复暂停。 |
| 实现提交与首次远端核对 | 通过；实现提交 `2a871c08cf035877cab60301f63f6c558ad32cf5` 已推送，`git ls-remote` 返回同一提交。 |

## 回退锚点

- 输入锚点：`2820297d55cbad46d4b525f74520da2271e022eb`。
- 实现提交：`2a871c08cf035877cab60301f63f6c558ad32cf5`，消息 `feat(m0): build core simulation foundation`。
- 该提交已经通过本机 `7897` 代理推送，首次远端核对返回相同提交；最终接力文档由本记录所在后续同步提交收口。

## 已知边界

- 当前界面只用于验证玩法底座，布局、字体、颜色、图标和交互样式不构成最终 UI 或美术接受。
- 当前真实工单只有`总部水务恢复`；地图、科研、勘测无人机、工厂、运输批次、故事和随机事故仍属于 402–404。
- M0 新存档与旧存档明确不兼容；旧键保持原样，可恢复但不会被 M0 读取。
- 低效拆解能防止普通零件软锁，但会持续损失总部可用物件；后续真实前哨来源仍应取代它。

## 唯一下一步

实现提交、代理推送和首次远端核对均已完成。当前只交给用户进行 M0-L4-401 功能试玩与接受判断；未得到该判断前不把任务标为 accepted，也不进入 M0-L4-402。
