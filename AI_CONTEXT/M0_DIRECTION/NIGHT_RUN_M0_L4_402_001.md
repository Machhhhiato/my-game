---
night_run_id: NIGHT-M0-L4-402-001
formal_task: M0-L4-402
output_id: BUILD-M0-CAPABILITY-001
run_status: implemented_waiting_future_playtest
input_commit: a3a11d76822a9149d2e6862c01a124747d25b03d
route_commit: 93b4d2b3413e5cdd49b216482f4eedea154b97ac
replacement_route_commit: 9b51c93dc868dd4fe39a0357213a237d6a3e1935
replacement_started_at: 2026-08-26T12:11:46+08:00
finished_at: 2026-08-26T13:01:51+08:00
first_layer_reviewed_at: 2026-08-26T13:05:33+08:00
first_layer_review_status: automatic_passed_visual_deferred_by_user
implementation_commit: d00f22c274ac1f582f7af8bbd7f439dcae8a2db3
---

# NIGHT-M0-L4-402-001 执行报告

## 本次结论

M0-L4-402 已在执行单白名单内完成接替修订，并通过第一层独立自动验收。当前状态为 `implemented_waiting_future_playtest`：它不是用户试玩接受，不代表浏览器视觉验收完成，也不进入 M0-L4-403。

正式实现基线为 `a3a11d76822a9149d2e6862c01a124747d25b03d`；接替时工作区位于路由提交 `93b4d2b3413e5cdd49b216482f4eedea154b97ac`，接替过程中第一层追加了记录接替实施的路由提交 `9b51c93dc868dd4fe39a0357213a237d6a3e1935`。第一层复核通过后，白名单代码已固化为实现提交 `d00f22c274ac1f582f7af8bbd7f439dcae8a2db3`；正式任务仍未由用户试玩接受。

## 首次实施失败与接替说明

首次 `/root/m0_l4_402` 使用 Terra/medium 留下了白名单内草稿，但第一层否决其成为完成品。首次报告正确记录了当时存在实质缺口；其 `started_at: 2026-08-26T12:00:00+08:00` 与 `finished_at: 2026-08-26T12:45:00+08:00` 没有可核对的真实运行证据，且后一个时间在本次接替实际完成时仍属于未来，不能继续作为证据。本报告保留首次失败事实，并以接替路由提交时间和本次实际停止时间替换时间证据。

接替线程 `/root/m0_l4_402_sol` 使用 gpt-5.6-sol/high，逐文件审计首次草稿后保留其正确底座，重写第一层指出的未完成机制。没有把首次旧的 `M0 stage A+B checks passed` 文案冒充 402 完成证据。

## 第一层首次退回与第二轮修订

第一层对接替版逐项审查后仍未接受，要求继续同一 402 白名单返工。退回证据包括：中央地图仍是平面六边板而不是冻结规格要求的可旋转球体；已批准调查隐藏配置、达到日限后无法在界面修改；无人机每次返航仍需要重复点击补能；自动科研没有严格表达“本轮入口 → 本轮普通科技 → 下一轮”；严格存档缺少若干跨状态一致性；报告需要继续保留退回链路。

第二轮针对以上问题完成：

- 新增无依赖球体外壳和局部球面投影，37 格明确标记为球面上的总部三圈内容窗口，不是整颗星球；有限 yaw/pitch 旋转支持指针拖动、方向键和 Home，背面及窗口外只保留无地点内容球壳；
- 调查在现场确认前始终显示人数、P 级、最大白昼和无人机设置；已批准状态只隐藏重复批准按钮，清空或提高日限可恢复已有项目，改为纯人工不清零；
- 初次补能仍需一次玩家批准；调查计划允许无人机后，每次实际出动返航自动把检查与补能提交维护队列，仍真实执行维护 2 人接入、被动过夜、维护 1 人检查；
- 自动科研选择器改为可注入定义集的纯选择边界，严格完成本轮入口、再补本轮普通科技、最后计算下一轮；本轮领域阻塞时可扫描同轮其他可执行领域，但不会进入更高阶段；
- 严格存档升级为 v4，补充球体旋转范围、科研 active/current 引用、调查阶段/路线/暂停与项目状态、无人机状态/补能/批准组合一致性和对应恶意存档测试。

## 完整读取回执

接替前已逐文件完整读到 EOF：

- `AGENTS.md`；
- `AI_CONTEXT/M0_DIRECTION/NIGHT_WORK_ORDER_M0_L4_402.md`；
- 冻结来源 `specs/spec-m0-map-001.md`、`specs/spec-m0-progression-001.md`、`specs/spec-m0-integrated-001.md`；
- `NIGHT_WORK_ORDER_M0_L4_401_R1.md`、`NIGHT_RUN_M0_L4_401_R1_001.md`、`PLAIN_LANGUAGE_RULES.md`；
- 首次失败报告 `NIGHT_RUN_M0_L4_402_001.md` 的原 45 行版本；
- 当时完整的 `src/m0/M0App.tsx`、`calendar.ts`、`economy.ts`、`m0.css`、`map.ts`、`progression.ts`、`save.ts`、`simulation.ts`、`state.ts`、`types.ts`；
- `scripts/m0-core-sim.ts`、`package.json`、`src/main.tsx`。

其中首次没有完整读完的 `spec-m0-progression-001.md` 与 `spec-m0-integrated-001.md`，本次均使用分段读取并确认最终 EOF；没有把搜索命中、摘要或截断输出记录成完整读取。

## 保留与重写范围

保留并复核：

- 以总部为中心三圈、共 37 个普通六边格的固定底座，稳定格 ID、对称邻接和八类固定地点锚点；
- 工业废墟 A、B 与未来农田的三种冻结选址结论及原因；
- 首次草稿中的科研、精密工坊、试制批、无人机实体数据结构；
- 401 已接受的公历逐日推进、月度连续资源账、人力守恒、一次性即时扣除、地图中心框架和事件窗行为。

接替后重写或补齐：

- 领域自动科研按玩家领域顺序选择下一阶段，透明保留较高领域阻塞；手动远期目标递归插入确定前置，前置完成或实体条件恢复后自动继续；切换手动/自动不清除已有进度与领域顺序；
- 勘测一次批准后保存并执行 2/4/6 人、P1–P3 和最大投入白昼，按优先级分配人力与唯一无人机，暂停、路线确认、日限和重载均保留部分进度；
- 未知格只使用统一玩家可见类名与标题，两处废墟开局不通过颜色、`title` 或 DOM 文本泄露位置；37 格使用局部球面投影叠加于可有限旋转的无内容球壳，旋转不改变稳定逻辑 ID 或情报；范围、路线、现场各自只显示本级已确认事实，结论只在事实充分时出现；科研领域、科技和情报阶段均使用中文可读名称，不向玩家显示内部枚举或 ID；
- 初次补能改为玩家只批准流程，随后占用维护 2 人接入、被动过夜、维护 1 人检查；这些人不同时计入日常维护产能；
- 科研、工坊修复、独立试制、无人机适配和无人机组装严格执行冻结人数、白昼、资源与单工坊互斥；试制产出的 4 个精密部件进入普通库存，组装使用 2 个后留下 2 个；
- 同一无人机只有方向到范围可独立完成；路线和现场各只减免 6 工作且必须有人工作完成剩余量；每目标每阶段减免最多一次，纯人工路线完整可行；
- v4 严格存档校验新增稳定格 ID、邻接对称与引用、固定路线、球体旋转范围、调查目标/阶段/项目引用与暂停一致性、科研领域/队列/active-current 引用、无人机任务引用和补能状态组合、重复 ID 与状态一致性校验；
- 定向测试逐项覆盖 402 完成标准，并把最终成功文案改为明确的 402 结果。

## 检查点结果

### A · 地图、情报与结论

- 固定 37 格、稳定 ID、对称邻接、固定锚点和同输入确定性：自动测试通过。
- 球面深度与缩放、有限旋转钳制、背面内容隐藏、37 格不冒充整颗星球、旋转不改变 ID/情报及旋转保存重载：自动测试通过。
- 未知废墟格的玩家可见 class 与 `title` 完全一致，方向/范围不提前暴露路线和资源事实：自动测试通过。
- A 为“适合”、B 为“有条件适合”、未来农田为“不可用”，且 A/B 只在现场确认后给出结论：自动测试通过。

### B · 科研

- 唯一科研线按 2/4/6 人分别推进 2/4/6 白昼工作：自动测试通过。
- 领域顺序、较高领域阻塞透明、其他同轮可执行领域接续、手动远期目标前置插入、实体前置阻塞和完成后自动继续：自动测试通过。
- 注入的无玩家内容测试定义证明阶段 1 普通科技先于阶段 2 入口，阶段 2 阻塞时不能越过到阶段 3；没有向正式科技表虚构普通科技。
- 手动与自动切换保留已做工作和玩家领域顺序；玩家侧科技、领域、情报标签不显示内部 ID 或枚举：自动测试通过。

### C · 实体链与无人机

- 精密工坊 9 人 × 4 日和 5/16/8 投入、试制 6 人 × 2 日和 2/4/4 投入、适配科研 6 人 × 3 日、组装 6 人 × 3 日和 3/10/8/2 投入：自动测试通过。
- 不足 9 人时工坊修复不产生部分进度，精密工坊同一时间只做一项工作：实现与测试通过。
- 初次补能不能瞬时完成且仍需玩家一次批准；后续每次真实无人机出动自动排入维护队列，维护接入、被动过夜、维护检查及维护人力不重复计算：自动测试通过。
- 唯一无人机优先分给较高优先级调查，不同时服务第二地点；三阶段减免各最多一次，后两阶段不能独立完成，纯人工 B 调查可完成：自动测试通过。

### D · 玩家界面

- 继续使用 401 顶部资源栏、左侧 sheet、中央地图和右侧事件窗；已接入地图、科研、工程、资产和地点 sheet。
- 地图按钮只接收情报过滤后的 class、可读 `aria-label` 与 `title`；球体支持防抖指针旋转和键盘有限旋转，常态弱化未知格线并突出选中、候选、路线和现场状态。
- 地点 sheet 分级显示已确认事实、路线选择、调查计划和结论；可测控制状态保证未批准时显示批准动作、已批准未完成时持续可编辑、现场确认后关闭计划编辑。
- 响应式 CSS、类型和生产构建已通过；按执行单未启动 dev server 或浏览器，因此桌面与窄屏最终视觉检查仍由第一层在允许启动后完成，本报告不把静态接线称为视觉验收。

### E · 存档与自动验证

- 即时试制产出、自动恢复科研、调查路线暂停/部分进度、最终科研/资产/调查状态均通过保存重载等价或次日确定性测试。
- 恶意存档样本覆盖重复格/路线/项目 ID、坏邻接、坏 selected-cell/route 引用、越界球体旋转、重复或非法科研领域、未知科研队列 ID、active-current 科研不一致、重复调查目标、方向阶段提前选路、调查暂停与项目状态不一致、坏调查阶段/项目引用、坏无人机任务引用及非法补能组合，均安全回到初始状态。

## 最终原始检查

- `npm run typecheck`：通过。
- `npm run test:m0`：通过；原始最终输出：`M0 stage A+B+402 checks passed: core accounting, spherical local window, strict capability saves, intelligence boundaries, automatic research rounds, entity work, queued drone recharge, and editable survey progression.`
- `npm run build`：通过；Vite 7.3.6 构建 38 个模块，生成 `dist/index.html`、CSS 与 JS 产物；本轮输出 CSS 12.82 kB、JS 271.05 kB。
- `git diff --check`：通过。
- `git diff -- src/m0/economy.ts`：无输出；白名单外 `economy.ts` 没有保留差异。
- 浏览器、dev server：按执行单明确未启动。

## 修改文件与边界

本批差异只涉及执行单白名单：

- 修改 `src/m0/types.ts`、`state.ts`、`simulation.ts`、`save.ts`、`M0App.tsx`、`m0.css`、`scripts/m0-core-sim.ts`；
- 新增 `src/m0/map.ts`、`src/m0/progression.ts`；
- 重写本报告 `AI_CONTEXT/M0_DIRECTION/NIGHT_RUN_M0_L4_402_001.md`。

没有进入 403：未做前哨、回运、标准工程包、物流批次、全球程序生成或新依赖；未部署、未发布，也未启动本地服务或浏览器。

## 回退与唯一下一步

- 正式回退锚点：`a3a11d76822a9149d2e6862c01a124747d25b03d`。
- 实施接替锚点：`93b4d2b3413e5cdd49b216482f4eedea154b97ac`；实现提交：`d00f22c274ac1f582f7af8bbd7f439dcae8a2db3`。
- 唯一下一步：保留当前构建，等用户以后愿意启动试玩时再做桌面与窄屏视觉、操作节奏和内容细调；本次不启动页面，也不进入 M0-L4-403。
