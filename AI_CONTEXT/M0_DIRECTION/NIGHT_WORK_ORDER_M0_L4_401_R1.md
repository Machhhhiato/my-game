---
night_run_id: NIGHT-M0-L4-401-R1-001
work_order_status: accepted
formal_task: M0-L4-401-R1
parent_task: M0-L4-401
accepted_source: M0-S003-U135
accepted_at: 2026-08-26T09:55:39+08:00
project_label: always game
real_repo: /Users/xujiangyue/AGENT Project/always game
branch: context/m0-direction
baseline_commit: 926a3fc6599a895018bf5c96d50db02ec630b192
input_commit: this_work_order_commit
output_id: BUILD-M0-CORE-001-R1
reasoning_effort: high
highest_reasoning_effort: high
first_attempt: /root/m0_l4_401_r1
first_attempt_result: failed_capability_clean_restore
replacement_model: gpt-5.6-sol
replacement_task: /root/m0_l4_401_r1_sol
replacement_started_at: 2026-08-26T10:24:03+08:00
finished_at: 2026-08-26T11:23:13+08:00
output_commit: dadcc22437a4a7914ebba39b3990fa5499c5b09d
run_report: NIGHT_RUN_M0_L4_401_R1_001.md
---

# NIGHT-M0-L4-401-R1-001 · 地图中心式正式界面与月度资源结算返工

## 任务身份

- 夜间批次编号：`NIGHT-M0-L4-401-R1-001`。
- 所属正式任务编号：`M0-L4-401-R1`；它是 `M0-L4-401` 的用户试玩返工，不进入 `M0-L4-402`。
- 唯一目标：保留用户已认可的基础结算数学关系，把玩家界面和时间结算重做为可延续到后期的地图中心式正式 UI 骨架，并采用真实公历、日级推进、月度持续资源结算。
- 预期产物：`BUILD-M0-CORE-001-R1`。
- 用户授权消息编号：`M0-S003-U135`；细节来源为 `M0-S003-U129` 至 `U134`，持续操作授权继续引用 `M0-S003-U126` 与 `D-M0-DIR-037`。

## 模型与线程

- 接替任务模型：`gpt-5.6-sol`。
- 初始思考深度：`high`；机械测试与明确核对可降到 `low`。
- 最高自动升级档：`high`；不得自动升到 `xhigh`、`max` 或 `ultra`。
- 升档依据：首次 `/root/m0_l4_401_r1` 已由 `gpt-5.6-terra / medium` 成功创建，但其压缩重写被第一层否决，清理后又明确无法安全完成核心迁移，属于已记录的能力失败，不是创建接口错误。
- 线程：依据 `D-M0-DIR-040`，先确认首次实例关闭且工作区恢复干净，再建立一个更合适模型的第四层接替任务。接替任务仍受本执行单全部范围、白名单和停止条件约束；不得同时保留两个活动实例。
- 第四层不得提交或推送；第一层负责独立审查、退回、补修、提交和推送。

## 项目与起点

- Codex 项目：`always game`。
- 真实仓库：`/Users/xujiangyue/AGENT Project/always game`。
- Host：Mac local。
- 分支：`context/m0-direction`。
- 输入提交：本执行单所在的授权同步提交；其父提交固定为 `926a3fc6599a895018bf5c96d50db02ec630b192`。第一层派发时必须把实际完整提交哈希写进任务消息，第四层用 `git rev-parse HEAD` 核对。
- 开始时工作区：必须干净，且本地分支与远端一致。
- 回退锚点：授权同步提交；既有实现提交 `2a871c08cf035877cab60301f63f6c558ad32cf5` 保留为旧界面对照。

项目归属、真实仓库、分支、输入提交或工作区任一不符时返回 `BLOCKED_ROUTING`。

## 已接受的玩家界面

1. 主画面以地图为中心；顶部为人口、六种资源、日期和速度；左侧为仅显示图标的系统入口；右侧没有固定资产概览栏。
2. `M0-L4-401-R1` 建立中央地图容器、尺寸和覆盖关系，不绘制假地图、不显示开发占位文字。`M0-L4-402` 以后直接把球体与 37 格窗口填入该容器。
3. 左侧至少保留总部、科研、工程、资产、地点、维修和档案入口。每个小板块只讲一个内容；同系统内容可以进入同一 sheet，但不再显示空洞的大总览标题。
4. 正在进行的工程只挂在工程 sheet；`总部水务恢复` 属于工程。高级资产进入资产 sheet，地点进入地点 sheet，总部设施与内部运行进入总部 sheet。
5. 人口位于顶部。平时只显示当前人口，固定预留变化位置；日结算实际变化后短暂显示净 `+N` 或 `-N`，数秒后淡出。读取存档不得重放旧变化。
6. 六种持续资源位于顶部，常驻显示当前库存和下次月度结算的预计净变化。悬停显示容量、结算日期、预计生产来源、预计需求来源、已累计、剩余期间预计和可维持时间。
7. 玩家不能编辑水、食物或零件安全线。系统依据实际人口、设施、工程与需求计算覆盖时间和内部停止条件；界面只展示事实。
8. 删除玩家侧和模型中的影子锁定/预留。一次性使用在发生时真实扣除；已经搬到现场的材料以后属于现场库存，不叫预留；未消耗、未搬走的资源保持自由可用。
9. 事件使用可拖动悬浮窗，默认在地图右侧；拖动位置持久化。只记录事实结果，不写每日资源算式、总结、建议、重要性判断或“需要注意”。不自动暂停，不用保姆式提示代替玩家观察。
10. 参考《群星》的地图中心信息层级，不复制其美术、图标、贴图或受版权保护的视觉资产。

## 已接受的日历与结算

1. 项目使用确定性公历：31 日月、30 日月、2 月 28/29 日和完整公历闰年规则；不得依赖系统时钟、时区或夏令时。
2. M0 测试剧本从一个由场景配置提供的平年 1 月 1 日开始；玩家看到年、月、日。不得把最终世界年份写死为未冻结设定。
3. 时间按日推进；标准速度仍为 20 秒/日，并提供暂停、1 倍、2 倍、4 倍。倍速仍按顺序处理每个游戏日，不得跨日跳过工程、科研、事故、人口变化或资源耗尽节点。
4. 科研、工程、维修、勘测、运输、事故和离散人口变化按日处理。
5. 水、食物、普通零件等持续生产与持续需求按月统一进入库存。一次性建造、维修或其他实际支出发生时立即扣除。
6. 月内生产或需求条件变化时，先把旧速率乘以已经经过的真实天数写入本月待结算账本，再按新速率预测剩余真实天数。实际库存只在进入下月 1 日时应用该月累计值。
7. 预计值在月初和实际输入发生变化时重算，不在每个普通日重新计算完整经济；界面渲染不得绑定每个日 tick 暴力重绘。
8. 水或食物可能在月末前耗尽时，系统计算最早耗尽日并安排该日进入真实短缺，不允许以月结为由让不存在的资源继续维持人口。输入改变后重算耗尽日；不弹警告，由顶部事实和事件窗反映后果。
9. 存档保存公历日期、本月待结算分段、当前预测、耗尽节点和 UI 悬浮窗位置；同状态重载必须得到相同结算结果。

## 明确非目标

- 不进入 `M0-L4-402`：不实现球体、37 格真实地图、地点选择、科研树、精密工坊或勘测无人机。
- 不进入 `M0-L4-403` 或 `404`：不实现前哨、运输复制、完整故事、随机事故批次或 90 日候选试玩。
- 不新增依赖，不复制《群星》视觉资产，不做最终美术，不删除旧模块、旧资源或旧存档，不部署、不发布、不合并分支。
- 不增加主动警报、推荐操作、自动暂停、每日总结或玩家可编辑安全线。

## 写入范围

- 允许：`src/m0/**`、`scripts/m0-core-sim.ts`、必要的 M0 定向测试文件、`src/main.tsx` 的 M0 入口接线、`package.json` 的定向测试命令，以及本执行单和相关 `AI_CONTEXT/M0_DIRECTION/**` 路由/报告文件。
- 禁止：白名单外旧玩法模块、旧 R38 数据、旧存档键、项目依赖、全局配置、Tauri bridge、部署和发布文件。
- 允许的外部写入：第一层本地提交和向 `context/m0-direction` 推送；不允许 merge、deploy、release、安装或账号配置修改。

## 时间与检查点

- 初始预计耗时：约 4–6 小时；仍在同一目标内且能完整收尾时可延长到 8 小时或更久。
- 检查点 A：确定性公历、月度待结算账本、事件驱动预测、月中变更与耗尽日测试通过。
- 检查点 B：顶部资源/人口、地图容器、左侧 sheet、工程归位和可拖动事件窗完成。
- 检查点 C：存档重载、速度、窄屏、生产构建和浏览器交互通过。
- 预计结束前约 1 小时停止新增功能，只做审查、测试、差异检查、报告、提交和推送。

## 完成标准

1. 旧版数学基准在新的月度口径下有明确换算测试，不能静默改坏 T02 供水、食物、维护、人力守恒和缺件后果。
2. 公历平年、闰年、28/29/30/31 日月、跨年和月初结算测试通过。
3. 月中至少两次产消变化能按真实经过天数分段累计；倍速与逐日推进最终结果一致。
4. 一次性支出即时扣除；不存在锁定/预留影子库存；水食物月中耗尽会在正确日期产生后果。
5. 玩家首屏以地图容器为中心，顶部人口/资源/时间永久可见，右侧无固定概览栏，工程只在工程 sheet。
6. 人口瞬时变化会淡出且不进存档；持续资源长期显示月度预计，悬停明细与账本一致。
7. 事件窗默认在右、可拖动、重载后位置一致；事件只写事实，不输出建议或每日资源总结。
8. 桌面和窄屏都能操作；不得使用 `innerHTML` 重建交互状态，频繁界面事件需按项目规则控制重绘。
9. `npm run typecheck`、M0 专用测试、`npm run build`、`git diff --check` 和真实浏览器验收全部通过。

## 立即停止条件

- 需要突破上述写入范围或作出未由 `U129` 至 `U135` 决定的新产品设计；
- 同一复杂根因连续两次修正失败；
- 新日历或结算规则与冻结事故、人口或前哨规格出现无法兼容的冲突；
- 需要新依赖、系统权限、登录、密钥、部署或破坏性操作；
- 上下文或运行条件不足以同时完成验证、报告和可回退 Git 收口。

## 次日交接

必须交付：实际耗时与停止原因、线程与退回过程、完成/未完成内容、修改文件、输入/输出提交、测试与浏览器证据、回退锚点、已知边界和唯一下一步。任务只能进入 `implemented_waiting_user_acceptance`，不得自行接受 `M0-L4-401`，不得自动开始 `M0-L4-402`。
